# Локальний Postgres (docker-compose.yml)
POSTGRES_USER=clotch
POSTGRES_PASSWORD=dev_only_change_me
POSTGRES_DB=clotch_crm
POSTGRES_PORT=5432

# Власник схеми — використовується для міграцій (drizzle-kit)
DATABASE_URL=postgres://clotch:dev_only_change_me@localhost:5432/clotch_crm

# Non-owner роль застосунку — під неї діють RLS-політики (див. docs/db.md)
APP_DATABASE_URL=postgres://app_user:dev_only_change_me_too@localhost:5432/clotch_crm

# TODO(auth): тимчасовий dev-тенант, доки нема сесій — див. src/server/tenant/get-tenant-id.ts
DEV_TENANT_ID=00000000-0000-0000-0000-000000000001