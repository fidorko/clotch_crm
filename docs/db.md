# DB

PostgreSQL 16 (Docker Compose, локально) + Drizzle ORM. Схема: `src/server/db/schema/*.ts`. Міграції: `drizzle/migrations/` (генеруються, комітяться в git, ніколи не редагуються вручну заднім числом).

## Мультитенантність (розділ 6 CLAUDE.md) — як дотримано

1. **`tenant_id` на кожній прикладній таблиці.** Єдиний виняток — `tenants` (сам довідник тенантів, глобальна таблиця за визначенням).
2. **Два незалежні рубежі захисту:**
   - Перший — явний `WHERE tenant_id = ...` у кожному запиті data-access шару (`src/server/data/*`).
   - Другий — PostgreSQL Row-Level Security: усі прикладні таблиці мають `ENABLE ROW LEVEL SECURITY` + політику `tenant_isolation` (`src/server/db/schema/rls.ts`), що звіряє `tenant_id` із сесійною змінною `app.tenant_id`.
3. **`tenant_id` — тільки з сервера.** Наразі немає авторизації/сесій, тому джерело тимчасове: `src/server/tenant/get-tenant-id.ts` (`DEV_TENANT_ID` з `.env`, позначено `TODO(auth)`). Клієнт (URL, форма) `tenant_id` ніколи не передає.
4. **Композитні індекси tenant_id-перші** — усюди, де є частий `WHERE`/`ORDER BY` (див. таблиці нижче).
5. **UNIQUE у межах тенанта** — `products (tenant_id, model_code)`, `product_skus (tenant_id, code)`, `product_skus (tenant_id, barcode)` (частковий, ігнорує `NULL`), `custom_characteristic_values (tenant_id, characteristic_id, value)`.
6. **Єдиний шар доступу** — `src/server/data/products.ts`. Усі функції приймають `tenantId` як обов'язковий типізований параметр; виклик без нього не компілюється.
7. **Перевірка належності перед дією** — кожен запит фільтрує і за `tenant_id`, і за `id`; чужий `id` під чужим `tenant_id` повертає `null`, а не помилку (перевірено, див. «Перевірка ізоляції» нижче).
8. **Суперадмін** — не реалізовано (немає авторизації взагалі), окремий шлях буде спроєктовано разом з ролями.

### Чому RLS реально працює, а не просто присутня в схемі
Власник таблиці (роль, якою котяться міграції — `DATABASE_URL`) обходить RLS за замовчуванням у Postgres. Тому застосунок підключається окремою **non-owner** роллю `app_user` (`APP_DATABASE_URL`), без прав власника, з явними `GRANT SELECT/INSERT/UPDATE/DELETE`. Роль створюється ідемпотентним скриптом `npm run db:setup-role` (`src/server/db/setup-app-role.ts`) — не вручну, щоб крок був відтворюваний на іншій машині/середовищі.

`src/server/db/client.ts` → `withTenant(tenantId, fn)` — єдина точка входу для тенант-скоупованих запитів: у транзакції виконує `set_config('app.tenant_id', tenantId, true)` (транзакційно, як `SET LOCAL`), потім викликає `fn`. RLS-політика читає це значення через `current_setting('app.tenant_id', true)::uuid` (`missing_ok = true` — якщо не виставлено, `NULL`, порівняння хибне, доступ забороняється).

**Fail-closed, перевірено:**
- Запит під власним `tenant_id` → дані повертаються.
- Той самий запит (той самий `id`) під чужим `tenant_id` → `null`/0 рядків.
- Пряме `SELECT` від `app_user` без `WHERE tenant_id` узагалі (перевірка, що RLS сама відсікає, незалежно від коду застосунку) — 0 рядків для чужого `app.tenant_id`, весь тенантний набір для свого.
- Якщо `app.tenant_id` не виставлено в сесії жодного разу — `current_setting(..., true)` повертає `NULL`, доступ заборонено. Якщо змінну виставляли раніше в тій самій сесії (інша транзакція) і не виставили в поточній — Postgres може повернути порожній рядок `''` для custom-GUC-плейсхолдера, і каст до `uuid` впаде з помилкою замість порожнього результату. Це теж fail-closed (дані не витікають), просто явна помилка замість тихої порожньої відповіді — у проді таке означає баг у виклику (забули `withTenant`), не витік.

## Таблиці

### tenants (глобальна, виняток із правила tenant_id)
| Поле | Тип | Коментар |
|---|---|---|
| id | uuid PK | |
| name | text NOT NULL | |
| created_at | timestamptz | |

Мінімальний склад навмисно — авторизації й керування тенантами ще немає.

RLS увімкнено (`ENABLE ROW LEVEL SECURITY`) без жодної policy — deny-all для будь-якої non-owner ролі; `tenantIsolationPolicy` тут не застосовна (нема власного `tenant_id`), а застосунок ніколи не читає цю таблицю через `app_user` (лише FK-посилання). Додано після переїзду на Supabase — публічна схема там автоматично видається назовні через PostgREST (Data API, ролі `anon`/`authenticated`), і Supabase-лінтер («RLS Disabled in Public») слушно зловив цю таблицю як єдину без RLS.

### products
Плоскі колонки (не jsonb) для `info.*`/`meta.*`/`pricing` — набір полів фіксований, не тенант-конфігурований, і потрібні звичайні `WHERE`/`ORDER BY`/індекси (ціна, статус, дата) — jsonb ускладнив би і те, і те без вигоди. Див. `decisions.md`.

Ключові поля: `category` (коротка назва) + `category_path` (повний ієрархічний шлях — раніше `info.category` в TS-типі, тепер розведено на дві колонки, `breadcrumb` не зберігається — рахується на рендері з `category` + `name`). Ціни — 4 канали (`retail`/`wholesale`/`dropship`/`retail_discount`), кожен `mode` + `amount` + `percent` (обидва значення зберігаються завжди, перемикач лише обирає активне — UI-патерн з `ProductPricingPanel`). `same_size_pricing` boolean, `NOT NULL DEFAULT true` (міграція `0038`, 2026-08-03) — перемикач у `ProductPricingPanel`: `true` — ціна одна на весь товар, `SKUDetail` не показує override-поля цін узагалі; `false` — кожен SKU може мати власну ціну (override-поля в `product_skus.*_override` лишаються, `SKUDetail` їх показує).

`category_id` (uuid, nullable) — реальний FK на `categories.id`, `ON DELETE SET NULL` (на відміну від `categories.parent_id`, де `RESTRICT` — тут інша семантика: видалення категорії не повинно блокуватись через товари, товар просто лишається без категорії). `category`/`category_path` — окремі, свідомо НЕ синхронізуються автоматично з обраною `category_id` (лишаються як були, для хлібних крихт і т. ін.) — відомий розрив, зафіксований нижче.

Індекси: `(tenant_id, created_at)`, `(tenant_id, status)`, `(tenant_id, category_id)` (під `GROUP BY category_id` для лічильника товарів на категорію). UNIQUE: `(tenant_id, model_code)` — модель-код унікальний у межах тенанта (підтверджено).

**`supplier_id` (uuid, nullable) — реальний FK на `suppliers.id`, `ON DELETE SET NULL`** (той самий патерн, що `category_id`). Замінив вільнотекстову колонку `supplier` (видалено). Дві окремі міграції (`0010` — DROP `supplier`, `0011` — ADD `supplier_id`), щоб `drizzle-kit generate` не розпізнав це як перейменування колонки в одному проході (та сама гочта, що з таблицями — `product_color_photos`, вище) — non-interactive shell не може відповісти на інтерактивний prompt.

### product_skus
`tenant_id`, `product_id` (FK `ON DELETE CASCADE`), `code`, `color`, `color_hex`, `size`, `barcode`, `stock` (CHECK `>= 0`), `cell`. Плюс 6 nullable `*_override` полів (грн, без `mode`/`percent` — SKU-панель має лише резолвнуту суму) — override не прив'язаний до окремого запису історії, `NULL` = успадковує ціну товару.

**Штрихкод — жорстка привʼязка до SKU** (пряма вказівка людини, 2026-08-03): `UNIQUE(tenant_id, barcode)` частковий (`WHERE barcode IS NOT NULL`) — уже було в схемі, тепер реально підключено в UI (`SKUDetail` → `updateSkuBarcodeAction` → `updateSkuBarcode`, `server/data/product-skus.ts`). Порожній штрихкод (`NULL`) — легальний проміжний стан, не конфліктує сам із собою. Порушення унікальності перетворюється на дружнє повідомлення («Штрихкод «Х» уже привʼязаний до іншого SKU»), той самий прийом, що колізія `code`. Мета — прийомка по скану ШК однозначно знаходить один SKU в межах тенанта; сама функція сканування/пошуку за ШК — не реалізована, лише гарантія унікальності на цьому рівні.

Індекси: `(tenant_id, product_id)`. UNIQUE: `(tenant_id, code)`, частковий `(tenant_id, barcode) WHERE barcode IS NOT NULL` (порожній штрихкод — легальний проміжний стан у конструкторі SKU-матриці, не повинен конфліктувати сам із собою).

**Відомий розрив, не виправлений навмисно:** TS-тип `ProductSku.cell` — одна комірка (текст), тоді як мок `selectedSku.cells` показує розбивку залишку по кількох комірках з кількістю (`[{code, qty}]`). Перший прохід БД лишає одну комірку на SKU, 1:1 з поточним типом. Багатокомірковість — окреме рішення, коли знадобиться складський модуль.

### product_photos — ВИДАЛЕНА (міграція `0037_regular_human_robot.sql`, 2026-08-03)
Окремих «фото моделі» більше немає: за прямою вказівкою людини фото товару **завжди** належить конкретному кольору й додається лише в таблиці варіантів (`ProductSkuTable`), тому дві таблиці для одного й того самого стали дублем. Уся робота з фото — через `product_color_photos` нижче; `ProductPhotoGallery` тепер лише показує їх із підписом кольору. Мініатюра в списку товарів (`listProducts`) теж перейшла на `product_color_photos` — перше фото першого кольору. Дані старої таблиці не переносились (рішення людини, `decisions.md`).

### product_color_photos
`tenant_id`, `product_id` (FK `products.id`, CASCADE), `color` (вільний текст — та сама копія значення, що й `product_skus.color`, **не FK**), `url`, `position`. Індекс `(tenant_id, product_id, color, position)`.

`is_main` boolean, `NOT NULL DEFAULT false` (міграція `0039`, 2026-08-03) — основне фото товару (`ProductPhotoGallery`, клік на зірку), глобальне на товар (не на колір). Частковий UNIQUE `(tenant_id, product_id) WHERE is_main` гарантує щонайбільше одне основне фото на товар навіть при паралельних запитах — не покладається лише на «спочатку скинути решту в false» у Server Action. Встановлення — два послідовні `UPDATE` в одній транзакції (`setMainColorPhoto`): усі фото товару в `false`, тоді обране в `true`.

**Фото прив'язане до кольору товару, не до окремого SKU.** На один колір завжди припадає кілька SKU (по одному на кожен розмір), і фото моделі в цьому кольорі однакове для всіх розмірів — прив'язка до `sku_id` (перший варіант цієї таблиці, `product_sku_photos`, замінений цією) змусила б завантажувати те саме фото окремо на кожен розмір і не показувала б його для розмірів, ще не доданих у матрицю. Наслідок: фото можна завантажити для кольору ще до того, як для нього створено хоч один SKU (немає залежності від `product_skus` узагалі, лише від `products.id` + вільний текст `color`) — і воно **не** каскадиться автоматично при видаленні SKU чи навіть цілого кольору з матриці (`color` не FK, FK тут лише на `product_id`). Видалення кольору цілком (`ProductSkuTable` → кошик біля назви кольору) тому явно чистить і фото цього кольору окремим запитом (`deleteColorPhotosByColor` + `deleteColorPhotosAction`), а не покладається на каскад.

Ліміт 3 фото на колір — **не** DB-constraint, перевіряється в Server Action (`uploadColorPhotoAction`) до збереження файлу на диск, щоб при відмові не лишати «осиротілий» файл.

### product_size_measurements
2026-08-02, замінила `product_measurements` (той не мав виміру розміру, `decisions.md`). `tenant_id`, `product_id` (CASCADE), `size` (вільний текст — той самий рядок, що `product_skus.size`, теж не FK, `db.md` нижче), `measurement_value_id` (**реальний FK** на `measurement_values.id`, CASCADE — тут, на відміну від `product_skus.size`/колишнього `product_measurements.type`, джерело точок заміру одне, тож FK доречний без багатоджерельної неоднозначності), `value_cm`, `updated_at`. PK `(tenant_id, product_id, size, measurement_value_id)` — рядок = «значення заміру для цього розміру товару». Індекс `(tenant_id, product_id)`. Точки заміру — лише ті, що закріплені за категорією товару (`measurement-type:*`, `characteristic-options.ts`), розміри — лише ті, що реально додані в SKU-матриці товару; UI — `ProductSizeChart.tsx`, `modules/products.md`.

### product_tags
Junction товар↔тег; `tenant_id` тут навмисно **денормалізований** (виводиться з `product_id`/`characteristic_value_id`), щоб буквально виконати правило «кожна прикладна таблиця має tenant_id» і щоб RLS-політика не залежала від join. `characteristic_value_id` — FK на `custom_characteristic_values.id` (раніше — на окрему таблицю `tags`, перенесено — деталі нижче, розділ «Теги»). PK `(product_id, characteristic_value_id)`, індекс `(tenant_id, characteristic_value_id)`.

### categories
Ієрархія категорій вітрини (модуль `settings`, вкладка «Категорії товару»): `tenant_id`, `parent_id` (самопосилання на `categories.id`, `NULL` = коренева), `name`, `description`, `image_url`, `is_active` («Активна на вітрині»), `show_in_storefront_section`, `show_in_header_menu`, `default_weight_kg`/`default_length_cm`/`default_width_cm`/`default_height_cm` (підставляються товару категорії, якщо там не вказано своє), `seo_h1`/`seo_meta_title`/`seo_meta_description`, `position` (для майбутнього drag-and-drop сортування — колонка є, сама функція сортування в UI ще не зроблена).

**Успадкування від батьківської категорії (за прямою вказівкою людини):** `is_active`/`show_in_storefront_section`/`show_in_header_menu` — **nullable** (міграція `0026`, раніше `NOT NULL DEFAULT true`). `NULL` = не задано на цій категорії, ефективне значення береться від найближчого предка з явним значенням (walk up `parent_id`, `resolveInheritedField` у `lib/categories/inheritance.ts`); якщо на всьому ланцюжку до кореня нічого не задано — застосунок трактує як `true`. Щойно людина торкається перемикача на конкретній категорії — значення стає власним НАЗАВЖДИ для неї (нема способу в UI скинути назад на «успадковано», не просили) і більше не залежить від подальших правок батька. `default_weight_kg`/`default_length_cm`/`default_width_cm`/`default_height_cm` вже були nullable й отримали ту саму семантику без зміни схеми: порожнє поле = успадковано. `name`/`description`/`image_url`/`seo_*`/`parent_id` **свідомо не успадковуються** — підтверджено з людиною (`AskUserQuestion`), не вигадано.

Індекси: `(tenant_id, parent_id)`, `(tenant_id, created_at)`. **UNIQUE на `name` навмисно нема** (не підтверджено з людиною, чи назва категорії унікальна навіть у межах тенанта — на відміну від `products.model_code`, де це підтверджено).

**`parent_id` — `ON DELETE RESTRICT`, не `SET NULL`/`CASCADE`.** Видалення категорії з дочірніми — заборонено на рівні самої БД (Postgres відхиляє DELETE, поки є діти), а не лише перевіркою в коді — так це діє, навіть якщо колись з'явиться інший шлях запису повз `src/server/data/categories.ts`. Захист від **циклу** в ієрархії (категорія стає батьком власного нащадка) — окрема перевірка в `updateCategoryAction` (`src/lib/categories/tree.ts` → `isDescendantCategory`), бо сам FK цього не ловить (це не рекурсивний CHECK).

**`products.category_id → categories.id` — підключено** (раніше цього розділу не було, товари й категорії були не пов'язані; тепер FK є, і колонка «Товарів» у таблиці категорій рахує реальні дані через `getProductCountsByCategory` — `GROUP BY category_id`, без N+1, і рекурсивно сумується по дереву на клієнті для батьківських категорій). `category`/`category_path` (вільний текст) при цьому **не оновлюються** автоматично при виборі `category_id` — два джерела даних про категорію товару поки існують паралельно, не синхронізовано. Це свідомий, обмежений за часом компроміс (не просили синхронізувати текстові поля), а не забута робота — але вартий уваги, якщо десь використовується `product.category`/`categoryPath` для відображення (напр. хлібні крихти `ProductHeader`) і очікується, що воно відповідає реально обраній категорії.

**Гочта Drizzle, яку ловили на цій таблиці:** коли `tx.delete(...)` падає через FK-порушення, Drizzle обгортає сиру помилку `postgres.js` у `DrizzleQueryError` — реальний Postgres-код (`error.code`, напр. `'23503'`) лежить у **`error.cause.code`**, не в `error.code` напряму. Перевірка `"code" in error` на верхньому рівні мовчки не спрацьовує (TypeScript цього не ловить, помилка лише в рантаймі — сира помилка Postgres просто йде користувачу замість дружнього тексту). Виправлено в `deleteCategory` (`server/data/categories.ts`).

### colors
Тенант-конфігурований довідник кольорів (`settings` → «Довідники» → «Кольори», відкривається попапом `ColorsFormDialog` з плитки `ColorsTile` — раніше була окрема сторінка `/settings/references/colors`, видалена), раніше — app-константа `COLOR_OPTIONS`. `tenant_id`, `name` (англійською), `hex` (`#RRGGBB`), `is_active`, `position`. UNIQUE `(tenant_id, name)` — дві однакові назви кольору в межах тенанта заборонені; на `hex` UNIQUE навмисно нема (два кольори можуть легітимно мати однаковий код, напр. синоніми назв). Індекс `(tenant_id, position)`.

`product_skus.color`/`color_hex` лишаються копією значення на момент створення SKU (не FK на `colors.id`) — як і раніше було задумано для `product_skus` (див. розділ вище): видалення кольору з довідника не ламає вже створені SKU заднім числом. Дев-сід (`npm run db:seed`) заповнює 30 найпоширеніших кольорів одягу (`lib/mocks/colors.ts`) — ідемпотентно (`onConflictDoNothing`, за UNIQUE-обмеженням).

### currencies
Тенант-конфігурований довідник валют (`settings` → «Довідники» → «Валюти»), окрема таблиця, не `reference_items` — валюті потрібні код/символ/позиція/знаки після коми/курс, не просто назва. `tenant_id`, `code` (ISO 4217, напр. `UAH`), `name`, `symbol`, `symbol_position` (enum `before`/`after`), `decimal_places`, `is_active`, `is_default`, `exchange_rate` (nullable, `numeric(14,6)` — курс відносно базової валюти тенанта; для самої базової завжди `NULL`, курс до себе = 1), `rate_updated_at`, `auto_update` (за замовчуванням `true`), `position`.

UNIQUE `(tenant_id, code)`. **`(tenant_id) WHERE is_default = true`** — частковий UNIQUE-індекс: лише одна валюта за замовчуванням на тенанта, підтверджено на рівні БД (не лише перевіркою в коді) — `createCurrency`/`updateCurrency` спершу знімають `is_default` з усіх інших валют тенанта в тій самій транзакції, тому проміжного стану «нуль базових валют» ніколи не видно ззовні. Видалення базової валюти заблоковано в data-access шарі (дружня помилка «спочатку призначте іншу базовою») — на рівні БД це не FK-обмеження, тож перевірка лише прикладна, на відміну від `categories.parent_id`.

**Курси — з НБУ** (`src/server/integrations/nbu.ts`, публічний API банку без ключа, `fetch` на `bank.gov.ua/NBUStatService/v1/statdirectory/exchange?valcode=...&json`). Дві точки виклику: одинична кнопка «Оновити курс» на рядку валюти й пакетна «Оновити курси» (усі з `auto_update = true`, крім базової). **Обидві — по кліку користувача, не за розкладом**: інфраструктури для реального cron/scheduled job (Vercel Cron, зовнішній тригер на API-роут тощо) у проєкті ще нема, і рішення, яку саме платформу деплою й спосіб планування використовувати, свідомо не прийнято мовчки — відкритий пункт нижче. Контракт полів НБУ (`cc`/`rate`/`exchangedate`) — задокументована й стабільна структура, але живий виклик не перевірявся в сесії розробки (без мережевого доступу з пісочниці) — перший реальний виклик з UI варто перевірити.

Індекс `(tenant_id, position)`. Дев-сід заповнює 4 стартові валюти (`lib/mocks/currencies.ts`): UAH (базова), USD, EUR, PLN — без курсу/дати оновлення (`NULL`), щоб не видавати вигадані дані за реальні з НБУ.

**`currencies` раніше був одним із 9 kind у `reference_items`** (нижче) — значення `"currencies"` лишається валідним у pg-enum `reference_item_kind` назавжди (Postgres не підтримує `DROP VALUE` для enum), але прибрано з `REFERENCE_ITEM_KINDS` (`lib/constants/reference-item-kinds.ts`), нових записів з таким `kind` більше не створюється; єдиний тестовий рядок, що встиг з'явитись до переходу, лишився в таблиці як нешкідливе сміття (не варте окремої міграції очищення).

### reference_items
Один спільний довідник для 3 «просто назва» пунктів «Довідники» (settings, група «Системні»): `manufacturers`/`countries`/`units` — замість окремої таблиці на кожен тип. `tenant_id`, `kind` (enum `reference_item_kind`, той самий список значень, що `REFERENCE_ITEM_KINDS` у `lib/constants/reference-item-kinds.ts` — тримати синхронно вручну, schema-файли навмисно не імпортують з `lib/`), `name`, `position`. UNIQUE `(tenant_id, kind, name)` — дублікат назви в межах одного kind заборонено, той самий кортеж не заважає різним kind мати однакову назву. Індекс `(tenant_id, kind, position)`.

**`collections`/`seasons`/`brands`/`fit` — БУЛИ тут, перенесені в `custom_characteristics`** за прямою вказівкою (разом із «Тегами», вище) — значення enum лишаються валідними в pg-enum `reference_item_kind` назавжди (Postgres не підтримує `DROP VALUE`), прибрано лише з `REFERENCE_ITEM_KINDS`/`REFERENCE_DEFS`, нових записів з такими `kind` більше не створюється. Перенесення виконано одноразовим скриптом (той самий прохід, що й для тегів, вище): 5 колекцій, 6 сезонів, 100 брендів скопійовано в `custom_characteristics`/`custom_characteristic_values`, старі рядки `reference_items` видалено.

**Свідомо НЕ увійшли в жоден спільний довідник:** «Інструкція по догляду» (потрібна ще й іконка на запис) і «Розміри»/«Заміри» (не просто список назв — власні таблиці `size_types`/`measurement_types`, нижче; підключені до картки товару 2026-08-02, `product_size_measurements`). **«Тип тканини» (`fabric-materials`) — БУВ тут, отримав власні таблиці** (значно багатший за «просто назва»: можливі матеріали, розтяжність, лицьова/тильна сторона) — нижче.

### custom_characteristics / custom_characteristic_values
Довідники, які власник магазину створює сам у рантаймі (плитка «+ Додати довідник» у групі «Характеристики товару», попап create/edit-в-одному). **Не потребує нової колонки чи таблиці на кожну нову характеристику** — саме для цього тут дві генеричні таблиці, а не розширення `reference_items.kind` (той — фіксований pg-enum, значення задаються в коді; `ALTER TYPE ... ADD VALUE` з коду застосунку на кожну довільну назву, яку вводить людина в попапі, — поганий шлях: DDL з боку користувача, неконтрольований ріст enum). Замість цього — класична модель «характеристика → її значення»:

- `custom_characteristics`: `tenant_id`, `name` (сама характеристика, напр. «Тип комірця»), `system_key` (nullable text, частковий UNIQUE `(tenant_id, system_key) WHERE system_key IS NOT NULL` — стабільний технічний ключ для характеристик, від яких залежить інша функціональність застосунку; єдине значення поки — `"tags"`, нижче), `show_in_crm`/`show_on_storefront`/`participates_in_filters` (booleans, за замовчуванням `true` — 3 перемикачі-іконки, тепер **прямо на плитці**, не в попапі), `position`. UNIQUE `(tenant_id, name)`.
- `custom_characteristic_values`: `tenant_id`, `characteristic_id` (FK, `ON DELETE CASCADE` — видалення характеристики видаляє й усі її значення), `value` (напр. «Суцільнокрійний»), `position`. UNIQUE `(tenant_id, characteristic_id, value)` — значення унікальне в межах своєї характеристики, той самий текст в іншій характеристиці — не конфліктує. Індекс `(tenant_id, characteristic_id, position)`.

Попап (`CustomCharacteristicFormDialog`) — і створення, і редагування: лише назва + значення (ввід-тегами — Enter додає чіп, X прибирає, як «Теги» на картці товару). 3 перемикачі-іконки (`Tooltip`) і кнопка-кошик — **на самій плитці** (`CustomCharacteristicTile`), не в попапі, за прямою вказівкою (щоб не відкривати попап заради швидкої дії); зберігаються окремим Server Action (`updateCustomCharacteristicFlagsAction`), не зачіпаючи назву/значення. Клік по плитці характеристики одразу відкриває попап редагування — окремої сторінки-списку більше нема (за прямою вказівкою: раніше був `/settings/references/custom/[id]`, прибрано). Кошик на плитці видаляє характеристику цілком (каскадно й значення); «Решту довідників» (усе, що лишилось у `REFERENCE_DEFS`/`reference_items`/`colors`/`currencies`/`suppliers`) видалити не можна — ця можливість є лише в custom_characteristics за визначенням.

### reference_dictionary_flags
Ті самі 3 перемикачі (`show_in_crm`/`show_on_storefront`/`participates_in_filters`), але для довідників групи «Характеристики товару», які **не** є `custom_characteristics` — «Кольори» (`colors`), «Тип тканини» (`fabric-materials` — ключ лишився з часів, коли плитка звалась «Тип тканини та матеріал», не перейменовували при розділенні 2026-08-02, `decisions.md`), «Матеріали» (`materials`, новий ключ, 2026-08-02 — раніше секція всередині попапу типу тканини), «Інструкція по догляду» (`care-instructions`), «Розміри» (`sizes`, новий ключ, 2026-08-02) і «Заміри» (`measurements`) — генерична тенант-скоупована таблиця, не залежна від наявності реального списку значень: `tenant_id`, `dictionary_key` (text, довільний стабільний ключ, без enum — нові ключі підключаються додаванням у список виклику `listDictionaryFlags`, без міграції схеми), `show_in_crm`/`show_on_storefront`/`participates_in_filters`, `updated_at`. PK `(tenant_id, dictionary_key)` (і сам за себе унікальний індекс, і фільтр tenant-first). Рядок з'являється лише після першого тоглу (`onConflictDoUpdate` upsert) — доки його нема, застосунок трактує довідник як `{true, true, true}` за замовчуванням у коді (`listDictionaryFlags`), не в БД.

**`updateCustomCharacteristic` (`server/data/custom-characteristics.ts`) переносить набір значень діфом, не `delete-then-insert` усього списку** — видаляє лише прибрані значення, додає лише нові, лишає незмінні з тими самими `id`. Критично для характеристики «Теги» (нижче): `id` значення — ціль FK з `product_tags`, і повне переприсвоєння `id` при кожному збереженні непомітно стирало б прив'язку тегів до товарів навіть коли набір значень фактично не змінився.

**Свідомо поза цим кроком:** підключення цих характеристик до самої картки товару (щоб їх можна було обрати як значення поля товару, крім «Тегів») — довідник існує сам собою, форма товару його ще не читає, як і для 4 «просто назва» довідників `reference_items` (не просили).

### category_characteristics
«Закріплення характеристик» (settings → сторінка категорії → вкладка «Характеристики», drag&drop за макетом людини, `modules/settings-category-characteristics.md`) — які характеристики закріплені за конкретною категорією, і в якому порядку. Охоплює **9 різних джерел** одразу (не тільки `custom_characteristics`): Кольори, Тип тканини та матеріал, Інструкція по догляду, кожен тип «Розмірів»/«Замірів» окремо (`size_types`/`measurement_types`, підтверджено з людиною — не весь довідник одним перемикачем), довільні `custom_characteristics`, і 3 «Системні» довідники за прямою вказівкою (Виробники/Країни — `reference_items`, Постачальники — `suppliers`; Валюти й Одиниці виміру свідомо не увійшли, не просили).

Замість FK на 9 різних таблиць — той самий принцип, що `reference_dictionary_flags`: **довільний стабільний текстовий ключ** `characteristic_key` — `"colors"` / `"fabric-materials"` / `"care-instructions"` / `"custom:<custom_characteristics.id>"` / `"size-type:<size_types.id>"` / `"measurement-type:<measurement_types.id>"` / `"reference-item:manufacturers"` / `"reference-item:brand-country"` / `"reference-item:country-of-origin"` / `"suppliers"`. Останні два — окремі незалежні поля товару («Країна бренду»/«Країна виготовлення»), обидва обирають зі СПІЛЬНОГО довідника `reference_items` (kind="countries") — не два довідники, один список назв країн, дві різні прив'язки на товарі (2026-08-02, за прямою вказівкою людини, `products-characteristics.md`).

`tenant_id`, `category_id` (FK `categories.id`, `ON DELETE CASCADE`), `characteristic_key`, `position` (integer — порядок картки в правій панелі, той самий, що вийшов з drag&drop), `updated_at`. PK `(tenant_id, category_id, characteristic_key)`. Рядок = «ця характеристика закріплена за цією категорією».

**Успадкування по батьківському ланцюжку категорій.** Наявність БУДЬ-ЯКОГО власного рядка на категорії — єдина ознака «ця категорія має власний закріплений список, більше не успадковує від батька»: будь-яка дія в drag&drop (додати/прибрати/переставити) пише **весь список наново**, одним `setCategoryPinnedCharacteristics` (delete-then-insert, `server/data/category-characteristics.ts`) — не по одному ключу. Читання — `listAllCategoryPinnedCharacteristics` (усі категорії тенанта одним батчем, не по одній: потрібен весь набір, щоб іти по дереву) + `resolveCategoryPinnedCharacteristics` (`lib/categories/characteristic-inheritance.ts`) — іде по `getCategoryPath` від категорії до кореня, повертає перший ВЛАСНИЙ (хоч раз збережений) список; якщо на всьому ланцюжку такого нема — порожньо (нова категорія без предків стартує з порожньої правої панелі — курований drag&drop-інтерфейс, не «увімкнено все за замовчуванням», інша семантика, ніж мала перша версія цієї картки). Категорія з явно порожнім власним списком (відкріпила все) невідрізнювана від категорії, яка ще не редагувала список — прийнятний компроміс (`decisions.md`), не просили окремого маркера.

**Раніше (до 2026-08, другий раунд) тут була пара таблиць** `category_characteristics` (з колонкою `enabled`) + `category_characteristic_excluded_values` (виключення конкретних значень) — прибрано повністю за новим макетом-скріном людини: рівень деталізації звузили до «характеристика цілком», вибір окремих значень усередині характеристики більше не підтримується в UI. Двоетапна міграція (`0027` — чисті дропи: `enabled`+таблиця винятків → `0028` — додано `position`), той самий прийом уникнення drizzle-kit ambiguous-rename prompt, що `0020`→`0021`/`0015`→`0016`.

Доступно лише для існуючих категорій (`/settings/categories/[id]`, не `/new` — потрібен `category.id`). **Підключено до картки товару** (2026-08-02, `modules/products-characteristics.md`) — три таблиці нижче.

### product_characteristic_values / product_material_composition / product_characteristic_layout
Значення динамічних характеристик на картці товару, за тим самим `characteristic_key`, що `category_characteristics` — `products-characteristics.md`.

- `product_characteristic_values`: `tenant_id`, `product_id` (FK CASCADE), `characteristic_key`, `value_id` (uuid, без FK — той самий компроміс, що `characteristic_key`: одна колонка не може посилатись на 4 різні таблиці-джерела одразу), `position`. PK `(tenant_id, product_id, characteristic_key, value_id)`, індекс `(tenant_id, product_id)`. Рядок = «товар має це значення для цієї характеристики»; більшість характеристик — один рядок, «Інструкція по догляду» — кілька (множинний вибір). `colors`/`suppliers`/`size-type:*`/`measurement-type:*` тут ніколи не зустрічаються — власний UI деінде (SKU-конструктор, панель метаданих, майбутня вкладка «Розміри та заміри»), виключені свідомо.
- `product_material_composition`: `tenant_id`, `product_id` (FK CASCADE), `material_id` (FK `materials.id`, CASCADE — на відміну від generic-таблиці вище тут завжди один конкретний тип джерела), `percent`, `position`. PK `(tenant_id, product_id, material_id)`, індекс `(tenant_id, product_id)`. Розкладка складу тканини (напр. «бавовна 90%, синтетика 10%») — сам обраний тип тканини лежить у `product_characteristic_values` (`characteristic_key = "fabric-materials"`), тут лише склад; матеріали обмежені «можливими матеріалами» обраного типу (`fabric_type_possible_materials`), перевірка на клієнті, не в БД.
- `product_characteristic_layout`: `tenant_id`, `characteristic_key`, `panel` (enum `characteristic_panel`: `info`/`meta`), `position`, `updated_at`. PK `(tenant_id, characteristic_key)`. **Тенант-рівнева, НЕ per-товар** — розподіл динамічних характеристик по `ProductInfoPanel`/`ProductMetaPanel` + порядок, редагується drag&drop прямо на картці товару («Редагувати розташування характеристик»), діє одразу для всіх товарів тенанта. Без власного рядка — дефолтна панель (Виробники/Країна бренду/Країна виготовлення/Теги → `meta`, решта → `info`, `resolveProductCharacteristicRows`), у кінці списку.

`products` втратила 9 колонок за два проходи цього кроку — усі були хардкод-копіями значень, не пов'язаними з жодним довідником, повністю замінені цими трьома таблицями: `brand`, `collection`, `season_type`, `fit`, `country_of_origin`, `manufacturer`, `material`, `fabric_type` (міграція `0029`), `brand_country` (міграція `0030`, окремо — «Країна бренду» стала динамічною характеристикою лише другим раундом, за прямим зауваженням людини після живої перевірки). `gender`/`description`/`season` лишились — не частина системи динамічних характеристик.

**Розміри/вага посилки товару (`package_length_cm`/`package_width_cm`/`package_height_cm`/`package_weight_kg`) успадковують ефективне значення категорії**, коли на товарі не задано власне (`NULL`) — той самий `resolveInheritedField`, що для `categories.default_*` (walk up `parent_id`), застосований ще на рівень нижче: категорія → товар. Колонки вже були nullable, нової міграції не знадобилось — лише нова інтерпретація `NULL` (як і для самих категорій) + мапер перестав підміняти `NULL` на `0`.

### product_technical_layout
2026-08-03, `products-technical-tab.md`. Тенант-рівневий (НЕ per-товар) порядок фіксованих полів вкладки «Технічні дані» — той самий принцип, що `product_characteristic_layout`, але без панелей: `tenant_id`, `field_key` (фіксований набір із `lib/products/technical-fields.ts`, не довільний текст), `position`, `updated_at`. PK `(tenant_id, field_key)`. Без власного рядка — дефолтний порядок, у кінці списку.

### product_activity_log
2026-08-03, `products-technical-tab.md`. Журнал подій товару: `tenant_id`, `product_id` (FK `products.id`, CASCADE), `event_type` (enum `product_activity_event`: `created`/`updated`), `occurred_at` (timestamptz, до секунди в UI), `actor_name` (`TODO(auth)`, той самий тимчасовий підхід, що `products.created_by`/`updated_by`), `field_key`/`field_label`/`old_value`/`new_value` (nullable — заповнені лише для `event_type = "updated"`, по одному рядку на кожне змінене поле; `NULL` для разової події `"created"`). Індекс `(tenant_id, product_id, occurred_at)`.

Рядки одного виклику `saveProduct` діляють однаковий `occurred_at` (рахується раз у коді, не `now()` в БД per-row) — так на екрані вони групуються в одну «подію редагування» з кількома змінами. Diff (`diffProductFields`, `server/data/product-activity-diff.ts`) охоплює лише прямі скалярні колонки `products` (назва/статус/категорія/постачальник/стать/опис/ціни/внутрішній артикул/артикул постачальника/розміри й вага посилки) — значення динамічних характеристик/складу тканини/замірів/тегів (junction-таблиці, `products-characteristics.md`) **не діфляться** (відкритий пункт). Номер поставки/номер замовлення — свідомо не додано, модулів «Постачання»/«Замовлення» ще нема в проєкті.

`products.created_by`/`updated_by` (колонки існували в схемі й раніше — розділ `products` вище) тепер реально заповнюються (`DEV_USER.name`) при створенні/збереженні — раніше лишались `NULL` поза сідом.

### Теги — тепер рядок custom_characteristics, не окрема таблиця
За прямою вказівкою (людина свідомо погодилась на ризик, `decisions.md`) стара таблиця `tags` + `product_tags.tag_id` **видалені**; теги товару — звичайна характеристика `custom_characteristics` з `system_key = 'tags'`, її значення — рядки `custom_characteristic_values`. `product_tags.tag_id` (FK на `tags.id`) замінено на `product_tags.characteristic_value_id` (FK на `custom_characteristic_values.id`, `ON DELETE CASCADE`), PK — `(product_id, characteristic_value_id)`.

`syncProductTags` (`server/data/products.ts`) — той самий find-or-create, що раніше був для `tags`, лише тепер: `getOrCreateTagsCharacteristicId` знаходить (або створює) характеристику за `system_key = 'tags'` тенанта, потім find-or-create значення в `custom_characteristic_values` цієї характеристики за міткою. Зовнішній тип `Product.tags: {id,label}[]` і весь UI картки товару (`ProductMetaPanel`/`TagsSection`) **не змінились** — лише джерело в БД.

**Чому `system_key`, а не пошук за назвою «Теги»:** людина може перейменувати відображувану назву характеристики в попапі — пошук за текстом зламав би прив'язку тегів до товарів мовчки. `system_key` — стабільний, не показується в UI, незалежний від перейменування.

**Двофазна міграція наявних даних (без втрати):** міграції `0015` (адитивна: нова `characteristic_value_id` поруч зі старою `tag_id`, нові колонки `custom_characteristics`) → одноразовий скрипт переносу (`tags`+`product_tags.tag_id` → `custom_characteristics`/`custom_characteristic_values`/`product_tags.characteristic_value_id`, той самий скрипт переніс і `reference_items` для `collections`/`seasons`/`brands`/`fit`, нижче) → міграція `0016` (руйнівна: `DROP TABLE tags`, `product_tags` без `tag_id`, `characteristic_value_id` — `NOT NULL` + PK). Перевірено на dev-даних: 4 теги / 4 зв'язки `product_tags` — жодного втраченого після переносу.

### materials
Тенант-конфігурований довідник матеріалів (`settings` → «Довідники» → «Матеріали», окрема плитка від 2026-08-02, раніше — секція всередині попапу «Тип тканини») — той самий патерн, що `colors`: `tenant_id`, `name`, `color` (nullable hex, суто ілюстративний зразок-кружечок), `category` (nullable enum `material_category` — `natural`/`cellulose`/`synthetic`/`leather`/`fur`/`rubber`/`other`, фіксований набір, той самий підхід, що `fabric_stretch`/`fabric_season` нижче: розробник задає значення в коді, не людина через попап), `position`. UNIQUE `(tenant_id, name)`, індекс `(tenant_id, position)`. З цього довідника обираються значення в `fabric_type_composition`/`fabric_type_possible_materials` нижче — видалення матеріалу каскадно прибирає його з усіх типів тканини, де він був використаний (`ON DELETE CASCADE`), без окремого попередження «де саме використовується» (лише загальний текст у діалозі підтвердження).

**Dev-сід — 55 матеріалів за прямим списком людини** (`lib/mocks/materials.ts`, `db:seed`), з розбивкою по 7 категоріях (натуральні/штучні целюлозні/синтетичні/шкіра/хутро/гума та подібні/інші). Дедуп при перенесенні списку в дані: «Штучна шкіра» дублювалась у категорії «Шкіра» (лишена раз), «ПВХ» фігурувала і в «Синтетичні», і в «Гума та подібні матеріали» (лишена лише в «Синтетичні» — `UNIQUE(tenant_id, name)` не дозволяє той самий матеріал у двох категоріях одночасно). Сід — `onConflictDoUpdate` (не `onConflictDoNothing`, на відміну від решти сідів): якщо матеріал з такою назвою вже існує (напр. людина встигла створити вручну через UI до запуску сіду), оновлюється лише `category`, `color`/`position` наявного рядка не чіпаються. **`category` використовується для групування пікера «Можливі матеріали»** (`FabricTypePossibleMaterialsField`, `MATERIAL_CATEGORY_OPTIONS` у `lib/constants/fabric-options.ts`) — матеріали без категорії (створені інлайн прямо з пікера, без вибору категорії в UI) групуються під «Інші» за замовчуванням.

### care_instructions
Тенант-конфігурований довідник рекомендацій по догляду (назва + іконка): `tenant_id`, `name` (напр. «Ручне прання»), `icon` (text-ключ із фіксованого набору-кандидатів `CARE_INSTRUCTION_ICON_OPTIONS`, `lib/constants/care-instruction-icons.ts` — 12 lucide-іконок), `position`. UNIQUE `(tenant_id, name)`, індекс `(tenant_id, position)`. Спочатку планувався для «Рекомендацій по догляду» на типі тканини, але людина усвідомила помилку моделювання (`decisions.md`) — реальний догляд залежить від фактичного складу конкретного товару, а не від назви структури плетіння тканини, тож прив'язку до `fabric_types` прибрано. **UI (settings → Довідники → «Інструкція по догляду»)**: плитка+попап `CareInstructionsTile`/`CareInstructionsFormDialog`/`CareInstructionsList` (той самий патерн, що «Кольори») — інлайн-редагування назви/іконки (`Select`), додати/видалити рядок. Server Actions (`createCareInstructionAction` тощо) вже існували «про запас» — лише під'єднано UI, без зміни бекенду. Dev-сід не додавали (немає жодного узгодженого з людиною списку інструкцій — довідник стартує порожнім).

### size_types / size_values, measurement_types / measurement_values
Довідник «Розміри та заміри» (settings → Довідники) — **два незалежні прості довідники**, не пов'язані між собою і без жодної таблиці-матриці (перша версія, з єдиною таблицею `sizes` — рядок = розмір + 6 колонок замірів, — виявилась неправильною моделлю, людина відхилила після демонстрації й попросила переробити; стара таблиця видалена окремою міграцією, `decisions.md`). Обидва — той самий "тип → список значень" патерн, що вже є в проєкті (`fabric_types`/`custom_characteristics`), тільки без 3 boolean-прапорців і без зв'язку з `product_tags` — тут це чисто довідникові списки.

- `size_types`: `tenant_id`, `name` (тип розміру, напр. «Взуття (EU)», «Джинси (обхват талії, дюйми)», «Бюстгальтери»), `position`. UNIQUE `(tenant_id, name)`.
- `size_values`: `tenant_id`, `size_type_id` (FK cascade), `value` (текстове значення розміру, напр. «42»), `position`. UNIQUE `(tenant_id, size_type_id, value)`.
- `measurement_types` / `measurement_values` — та сама пара таблиць, інша семантика: `value` тут — назва точки заміру (напр. «Обхват грудей»), не сам розмір.

**Типи — тенант-керовані** (підтверджено з людиною): власник магазину сам додає/перейменовує/видаляє типи, а не фіксований enum у коді. Збереження значень типу — `delete-then-insert` усього набору (не діф, як у `custom_characteristics`): нічого зовнішнього не посилається на конкретний `id` значення, тож захищати стабільність id не потрібно.

UI — дві окремі плитки `SizesTile`/`MeasurementsTile` (2026-08-02, розділено — раніше одна комбінована плитка з `Tabs`, `settings-references.md`), кожна свій попап (`SizeTypesFormDialog`/`MeasurementTypesFormDialog`) зі спільним компонентом `TypeValuesEditor` (картка на тип: назва інлайн-редагована, значення — чипи з input+Enter, як у `CustomCharacteristicFormDialog`), різні Server Actions.

**Dev-сід** (`lib/mocks/size-types.ts`/`measurement-types.ts`, `db:seed`) — 6 типів розмірів/71 значення і 5 типів замірів/17 значень, зведені з відкритих джерел про типові системи розмірів і точки заміру одягу (не вигадані навмання) — стартова колекція, не вичерпний перелік.

**Картка товару підключена 2026-08-02** — `ProductSizeChart.tsx` тепер реальна сітка «розмір × точка заміру» на основі цих двох довідників (`product_size_measurements`, вище), `modules/products.md`.

### fabric_types / fabric_type_possible_materials
Довідник «Тип тканини та матеріал» (`/settings/references/fabric-materials`, вкладка «Типи тканини») — замінив колишній `reference_items` kind=`fabric-materials` («просто назва») карткою з детальними властивостями за зразком-скріном людини.

**Модель навмисно НЕ зберігає % склад і не прив'язує рекомендації по догляду до типу тканини** (`decisions.md`): «тип тканини» (Футер, Кулір...) — назва структури плетіння, а не рецепт. Та сама структура в різних партіях/товарах має різний фактичний склад волокон, тож і точний % складу, і догляд (який залежить саме від фактичного складу) — властивість конкретного товару, майбутня задача, не довідника типу тканини. На рівні типу тканини лишається лише **«Можливі матеріали»** — необов'язковий перелік того, що типово зустрічається в цій структурі (не рецепт, не %).

- `fabric_types`: `tenant_id`, `name`, `code` (text, автогенерований slug — спрощена транслітерація кирилиці в латиницю, напр. «Футер 3-нитка» → `futer-3-nitka`, не офіційний стандарт, лише читабельний URL-безпечний код; унікальність — ретрай із суфіксом `-2`/`-3`...; **код ніде не показується як інпут** — генерується сервером, лишається виключно для URL-безпечного ідентифікатора), `description` (короткий опис), `density` (text, вільний формат — напр. «320-340», підпис одиниці «г/м²» — на рівні UI, не в даних), `stretch` (nullable enum `fabric_stretch`: `low`/`medium`/`high`), `front_side`/`back_side`/`tactile_feel` (Лицьова сторона/Тильна сторона/Тактильні відчуття — вільний текст, усі необов'язкові), `is_active`, `position`. UNIQUE `(tenant_id, name)` і `(tenant_id, code)`, індекс `(tenant_id, position)`.
- `fabric_type_possible_materials`: `tenant_id`, `fabric_type_id` (FK на `fabric_types.id`, cascade), `material_id` (FK на `materials.id`, cascade). PK `(fabric_type_id, material_id)`, `tenant_id` денормалізовано (як `product_tags`), щоб RLS не залежав від join, індекс `(tenant_id, fabric_type_id)`.

Збереження типу тканини (`updateFabricType`) — `delete-then-insert` набору `fabric_type_possible_materials` (`syncPossibleMaterials`, `server/data/fabric-types.ts`) — значення тут ніде не є ціллю зовнішнього FK з іншої таблиці, тож id-черн при кожному збереженні нікому не шкодить.

**Раніше (до 2026-08) тут існували `fabric_type_composition` (матеріал+%), `fabric_type_seasons` (`fabric_season` enum) і `fabric_type_care_instructions`** — прибрані повністю двоетапною міграцією (`0020` адитивна: нові колонки `front_side`/`back_side`/`tactile_feel` + `DROP TABLE` трьох таблиць/enum одразу, без ризику ambiguous-rename, бо додавання й видалення торкались різних таблиць → `0021` руйнівна: `DROP COLUMN schema_image_url`/`schema_notes`/`recommended_use` окремим кроком, щоб уникнути drizzle-kit rename-prompt на одночасний add+drop у тій самій таблиці `fabric_types`, той самий прийом, що `0015`→`0016`). Втрачено лише тестові дані одного dev-запису («Футер 2», ручне тестування) — сам тип тканини й прив'язаний можливий матеріал збереглися.

Список типів тканини (`listFabricTypesWithDetails`) віддає одразу все з id можливих матеріалів (один batch-запит, не по одному на вибір у списку — компоненти резолвять назву/колір із окремого `materials`-пропу) — клієнт сам фільтрує/пагінує (по 10) серед уже завантажених даних, без client-side data-fetching бібліотеки в проєкті й без окремого запиту при виборі рядка. Обсяг тенантного довідника малий, тож це прийнятно; за реального масштабу (сотні типів) варто перейти на серверну пагінацію.

Зображення схеми тканини — реальне файлове сховище, той самий патерн, що `category-images.ts`: `server/storage/fabric-type-images.ts` + `api/uploads/fabric-types/[filename]/route.ts`, диск поза webroot, `tenantId` роздачі — з сесії (`getDevTenantId()`), не з URL.

### suppliers / supplier_contacts / supplier_channels / supplier_custom_fields
Довідник постачальників (`settings` → «Довідники» → «Постачальники»). `suppliers`: `tenant_id`, `name`, `code` (генерується сервером, `SUP-0001`...; UNIQUE `(tenant_id, code)` — генерація рахує поточну кількість +1, при рідкісній гонці `createSupplier` ловить `23505` і повторює з наступним номером, до 5 спроб), `type` (enum `manufacturer`/`distributor`/`wholesaler`/`importer`/`other`), `is_active`, `website`, `country`/`city`/`address`, `notes`. Індекс `(tenant_id, created_at)`.

**`postal_code` видалено** (міграція `0009`, `ALTER TABLE suppliers DROP COLUMN postal_code`) — поле забрали з форми за прямою вказівкою людини («Індекс не потрібен») одразу після створення таблиці, дані ще не встигли з'явитись, тож дропнули колонку, а не лишили осиротілою.

Три дочірні таблиці — усі `supplier_id` FK `ON DELETE CASCADE`, `tenant_id` денормалізовано (як `product_tags`, щоб RLS не залежав від join), індекс `(tenant_id, supplier_id, position)`:
- `supplier_contacts` — кілька контактних осіб на постачальника: `name`, `job_title`, `phone`, `email`.
- `supplier_channels` — месенджери й соцмережі одним рядком: `kind` (enum `messenger`/`social`), `channel` (вільний текст: `telegram`/`viber`/`whatsapp`/`facebook`/`instagram`/`tiktok`/`threads` — не enum, бо фіксований набір живе в UI-довіднику `lib/constants/supplier-options.ts`, не є окремою БД-сутністю), `value`.
- `supplier_custom_fields` — довільні пари «назва поля — значення» в блоці адреси, людина сама називає поле: `label`, `value`.

Збереження форми — **delete-then-insert** для всіх трьох дочірніх колекцій одразу (`updateSupplier`, `server/data/suppliers.ts`), той самий патерн, що `syncProductTags` у `server/data/products.ts`: просто й коректно для «зберегти всю форму одним запитом», без per-рядкового diff.

**Іконки месенджерів/соцмереж — справжні бренд-лого через `react-icons`.** `lucide-react` підтверджено не має брендових іконок (Facebook, Instagram, Telegram, Twitter, Youtube, Linkedin, Github — усі відсутні). Погоджено з людиною (`AskUserQuestion`) додати `react-icons` (набір `fa6`, tree-shakeable) замість самописних SVG — кожна іконка в кольоровому кружечку брендового кольору (`lib/constants/supplier-options.ts`). Деталі — `decisions.md`.

### warehouses
Довідник складів (`settings` → «Склади», `settings-warehouses.md`). `tenant_id`, `name`, `code` (генерується сервером, `WH-0001`...; UNIQUE `(tenant_id, code)`, той самий ретрай-патерн на `23505`, що `suppliers`), `type` (enum `warehouse_type`: `main`/`pos`/`returns`/`defective`/`disposal`/`production`), `is_active`, `responsible_person`, `responsible_phone` (нормалізований `+380XXXXXXXXX`, conventions.md), `country`/`city`/`address`, `notes`, `work_hours` (`jsonb`, довільний список `{id,label,isDayOff,from,to}` — не окрема таблиця, 3-5 рядків на склад), `currency_code`, `can_sell`/`allow_negative_stock`/`use_bin_locations` (boolean). Індекс `(tenant_id, created_at)`.

**Країна/валюта — копія значення (текст/код), не FK** на `reference_items`/`currencies` — той самий принцип, що `colors`→`product_skus` і `suppliers.country`: видалення довідникового значення заднім числом не ламає вже створений склад.

**Адресне зберігання** — `bin_level1_name`/`bin_level2_name`/`bin_level3_name` (назви рівнів, за замовч. «Вулиця»/«Стелаж»/«Комірка» — редагуються інлайн у колонці `WarehouseBinExplorer`, перегляд+олівчик), `bin_level1_format`/`bin_level2_format`/`bin_level3_format` (текстовий ЗРАЗОК стартового значення — цифровий `"101"`/`"01"` задає старт+ширину падінгу нулями, буквений `"A"` задає старт літери, послідовність Excel-стилем; парсинг — `src/lib/warehouse/bin-address.ts`; використовується лише при масовому створенні партії — «Додати декілька», запам'ятовується після кожного використання). **2026-08-03, другий прохід:** `bin_generate_barcodes`/`bin_generate_qr`/`bin_streets_count`/`bin_racks_per_street`/`bin_cells_per_rack` — прибрано (bulk-генератор замінено на ручну 3-колонкову структуру). **2026-08-03, третій прохід:** `bin_separator`/`warehouse_bin_separator`-enum і `bin_allow_label_reprint` — прибрано (роздільник тепер фіксований пробіл у `composeBinCode`, чекбокс повторного друку ніде не використовувався функціонально).

### warehouse_bin_streets / warehouse_bin_racks / warehouse_bin_cells
**2026-08-03, другий прохід** — замінили плоску `warehouse_bin_locations` (видалена): пряма вказівка людини створювати структуру складу вручну по рівнях (`settings-warehouses.md`, `WarehouseBinExplorer`), не крос-добутком. 3 таблиці батько→нащадок (той самий принцип, що `size_types`/`size_values`):

- **`warehouse_bin_streets`** — `tenant_id`, `warehouse_id` (FK `warehouses.id`, `ON DELETE CASCADE`), `value` (текст рівня, напр. `"101"`). `UNIQUE(tenant_id, warehouse_id, value)`, індекс `(tenant_id, warehouse_id)`.
- **`warehouse_bin_racks`** — `tenant_id`, `warehouse_id` (денормалізовано, як і в старій таблиці — прямі запити без join), `street_id` (FK `warehouse_bin_streets.id`, `ON DELETE CASCADE`), `value`. `UNIQUE(tenant_id, street_id, value)`, індекс `(tenant_id, street_id)`.
- **`warehouse_bin_cells`** — `tenant_id`, `warehouse_id`, `rack_id` (FK `warehouse_bin_racks.id`, `ON DELETE CASCADE`), `value`, `code` (повна адреса, складена `street.value`+`rack.value`+`value` через роздільник складу), `barcode` (= `code`, значення для справжнього Code128 — `bwip-js`, `server/warehouse/bin-label-pdf.ts`, погоджено з людиною як нова залежність, `decisions.md`). `UNIQUE(tenant_id, rack_id, value)` + `UNIQUE(tenant_id, warehouse_id, code)`, індекси `(tenant_id, rack_id)`/`(tenant_id, warehouse_id)`.

Видалення вулиці/стелажа каскадно зносить нащадків через `ON DELETE CASCADE` у ланцюжку — сам Postgres, без рекурсії в коді. Значення рівня (наступне у форматі-послідовності) рахує `nextSequenceValues` (`src/lib/warehouse/bin-address.ts`) — фільтрує вже зайняті, коректно і після видалень-з-дірками.

### receiving_documents / receiving_document_custom_fields / receiving_document_items
**2026-08-04, warehouse-receiving.md** — реальний бекенд модуля: заголовок документа, довільні поля людини, і (восьмий прохід) реальні позиції.

- **`receiving_documents`** — `tenant_id`, `number` (генерується сервером, `RCV-2026-001`...; `UNIQUE(tenant_id, number)`, генерація за **максимальним наявним номером цього року**, не `count()` — див. гочту нижче), `type` (enum `receiving_document_type`: `planned`/`actual`), `status` (enum `receiving_document_status`: `draft`/`expected`/`posted`, за замовч. `draft` — **у списку більше не показується напряму**, витіснений похідним статусом з позицій, дивись нижче), `based_on_id` (id планового документа для фактичного, створеного на його основі — **без FK-обмеження**, самопосилання в drizzle вимагає окремого workaround, і звʼязок проставляється лише через `createActualReceivingFromPlanned`), `supplier_id` (FK `suppliers.id`, nullable), `warehouse_id` (FK `warehouses.id`, nullable), `planned_date` (`date`, nullable — конвертація з `DD.MM.YYYY` на межі УІ, `DateInput`/`parseDateInputToIso`), `supplier_document`, `ttn_carrier` (enum `receiving_ttn_carrier`: `nova_poshta`/`ukrposhta`, nullable), `ttn_number`, `responsible_person` (вільний текст — нема таблиці користувачів, `TODO(auth)`), `comment`. Індекс `(tenant_id, created_at)`.
- **`receiving_document_custom_fields`** — довільні пари «назва поля — значення» на документі (людина сама називає поле) — той самий принцип, що `supplier_custom_fields`, лише прив'язка до `document_id` (FK `receiving_documents.id`, `ON DELETE CASCADE`) замість постачальника. `label`, `value` (nullable), `position`. Індекс `(tenant_id, document_id, position)`.
- **`receiving_document_items`** (восьмий прохід) — позиції одного надходження: `document_id` (FK `receiving_documents.id`, `ON DELETE CASCADE`), `product_sku_id` (FK `product_skus.id`, **без `onDelete`** — SKU, що фігурував у надходженні, видалити не можна, той самий навмисний RESTRICT, що `supplier_id`/`warehouse_id` вище), `ordered`/`received` (integer, `CHECK >= 0`), `position`. **Без денормалізованих полів** (назва/колір/розмір/ШК/фото) — усе джойниться живцем з `product_skus`+`products` (`listReceivingDocumentItems`, той самий принцип, що `listProductSkusCatalog`). `UNIQUE(tenant_id, document_id, product_sku_id)` — повторний скан того самого SKU оновлює рядок (`ON CONFLICT DO UPDATE`), не плодить дублі. Індекс `(tenant_id, document_id, position)`.

**Гочта з генерацією номера (виправлено після live-перевірки):** початкова версія рахувала `count(*)+1` — якщо документ видалили, лічильник більше не відповідає найвищому реально зайнятому номеру («є лише №002, №001/№003+ видалено» → `count()=1` → знову й знову генерує зайнятий «002»). Ретрай на `23505` теж не рятував: Postgres абортує ВСЮ транзакцію після будь-якої помилки (включно з конфліктом унікальності), тож наступний запит у тій самій `tx` одразу падає з «current transaction is aborted» — спроба «зловити помилку і продовжити в тій самій транзакції» неробоча (та сама гочта потенційно є і в `generateWarehouseCode`/`createWarehouse`/`createSku`, там просто ніколи не траплялась дірка). Виправлено двома речами: (1) номер тепер — `MAX(number)` (з `LIKE 'RCV-2026-%'`) `+1`, не `count()`; (2) кожна спроба вставки — окрема транзакція (`withTenant` викликається заново на кожен `attempt`), не одна спільна.

**`product_skus.stock` — перше місце в проєкті, де він реально змінюється** (`updateReceivingDocumentItemReceived`/`upsertReceivingDocumentItem` з `qtyField="received"`, `server/data/receiving-items.ts`): дельта `received` одразу додається до `stock` в тій самій транзакції (пряме рішення людини — «Прийнято 10» = залишок цього SKU +10 негайно). Видалення рядка з ненульовим `received` відкочує стільки ж назад.

Автозбереження форми (`PlannedReceivingWorkspace`) — перше явне «Зберегти» створює документ (`createReceivingDocumentAction`), кожна наступна зміна поля автозберігається через `updateReceivingDocumentAction` (debounce ~500мс, той самий принцип, що картка товару, `conventions.md`). Кастомне поле — «+ Додати поле» неявно створює документ, якщо його ще нема (той самий `ensureDocumentId`), тоді додає рядок. Сканування/додавання SKU — миттєво, без debounce (свідома дискретна дія); ручне редагування кількості — debounce, той самий принцип, що заголовок.

**Сервер-гард (не лише UI):** `updateReceivingDocumentItemOrdered` no-op, якщо документ не `type='planned'`; `updateReceivingDocumentItemReceived` no-op, якщо не `type='actual'` — перевірка в самій функції, до запису.

**Похідний статус у списку** (`computeReceivingDocDisplayStatus`, `lib/types/receiving.ts`) — не сирий `status`-enum, а порівняння `SUM(ordered)`/`SUM(received)` по позиціях документа (агрегат рахує `listReceivingDocuments` окремим `GROUP BY document_id` запитом): «Очікується поставка» (жовтий) / «Прийнято повністю» (зелений) / «Розбіжність в плюс» (синій) / «Розбіжність в мінус» (червоний) / «Є непланові товари» (фіолетовий, коли є хоч одна позиція з `ordered=0` — додана понад план). Синій і фіолетовий — єдині кольори поза палітрою `design.md`, додані за прямим проханням людини (Tailwind `blue-*`/`purple-*` напряму, без нового CSS-токена — лише два одноразові використання).

## Що свідомо НЕ нормалізовано зараз (і чому)
Розміри (`SIZE_OPTIONS`), типи замірів (`MEASUREMENT_TYPE_OPTIONS`) — лишаються app-константами, не таблицями. Причини: (1) роадмап цього проходу обмежений `products`/`product_skus` + логічно пов'язаним; (2) список поки спільний для всіх тенантів — таблиця зараз стартувала б як ще один глобальний виняток без реальної потреби; (3) `product_skus.size` вже зараз — копія значення на момент створення SKU, не посилання. Кольори вже нормалізовані (`colors`, вище) — той самий перехід, коли знадобиться, підходить і для розмірів: `sizes(id, tenant_id, label)` з FK.

**Оновлення:** `products.supplier_id` тепер реальний FK на `suppliers.id` (вище, розділ `products`) — раніше тут описувалось як «майбутня задача», зроблено. Фільтра товарів за постачальником у `listProducts` ще нема (окремий індекс `(tenant_id, supplier_id)` не додавали — немає поки запиту, що його потребує).

## Файлове сховище (завантажені зображення)
**Оновлення:** початково зображення писались на диск (`src/server/storage/*.ts`, `<корінь>/storage/uploads/...`) — цей шар видалено. Тепер байти зберігаються прямо в БД (колонка `data` типу `bytea`, кастомний тип `src/server/db/schema/custom-types.ts`), диск і `/storage/` більше не використовуються. Причина: єдиний Postgres (Supabase) — спільний для всіх машин розробки, тоді як диск локальний для кожного ПК; файли на диску губились би при переїзді/новому середовищі, байти в БД — ні.

Таблиці з байтами: `product_color_photos.data`/`mime_type` (вище; `product_photos` видалена — там-таки), `category_images` (нова, нижче). Валідація файлу (розмір ≤ 5 МБ, MIME-білий список `image/jpeg`/`image/png`/`image/webp`) — спільна функція `validateImageFile` (`src/server/images/validate-image.ts`), викликається перед вставкою байтів у БД.

Роздача — через route handlers `src/app/api/uploads/{categories,product-colors}/[id]/route.ts`: `id` у URL — це `id` рядка таблиці (не `tenant_id`, не шлях на диску), маршрут сам бере `tenantId` із `getDevTenantId()` (сервер) і читає рядок саме цього тенанта (`readCategoryImage`/аналоги в `product-color-photos.ts`, фільтр `tenant_id` + `id`). Чужий тенант, знаючи чужий `id`, отримає 404 (рядок під його `tenant_id` просто не знайдеться) — той самий принцип ізоляції, що й скрізь у БД, через RLS + явний фільтр, а не через файлову систему. Відповідь — байти напряму (`new Response(new Uint8Array(data), { headers: { "Content-Type": mimeType, "Cache-Control": "private, max-age=3600" } })`).

### category_images
Окрема таблиця, не колонка на `categories`: зображення завантажується одразу при виборі файлу (жива мініатюра ще до збереження форми категорії) — чернетка `/new` ще не має `categories.id`. `tenant_id`, `data` (`bytea`), `mime_type`, `created_at`. RLS увімкнено з `tenantIsolationPolicy` (на відміну від `tenants` — тут є власний `tenant_id`, тож звичайна ізоляційна політика застосовна). `categories.image_url` лишається text-посиланням на `/api/uploads/categories/<id>`, сама схема `categories` не змінилась.

**Гочта, яку ловили тут (загальний принцип лишається актуальним, конкретний приклад — ні):** константу не можна імпортувати в клієнтський компонент із `server/data/*` — навіть якщо тягнеться лише значення константи, весь модуль (а з ним `postgres`) потрапляє в клієнтський бандл і валить збірку. Такі константи, потрібні і клієнту, і серверу, — окремий файл у `lib/constants/`, не `server/*`. (Конкретний приклад, що це ловив, — ліміт кількості фото на колір — прибрано повністю за прямою вказівкою людини, 2026-08-03; принцип лишається на майбутнє.)

**Гочта з drizzle-kit generate при перейменуванні таблиці:** коли стара таблиця (`product_sku_photos`) видаляється зі схеми, а нова (`product_color_photos`) з подібною формою додається в тому самому проході — `drizzle-kit generate` розпізнає це як можливе перейменування і намагається запитати інтерактивно («це перейменування чи нова таблиця?»), що падає в non-TTY середовищі (`Interactive prompts require a TTY terminal`). Обхід — генерувати в два окремі проходи: спершу видалити стару таблицю зі схеми й згенерувати/накотити суто DROP-міграцію, потім додати нову таблицю окремим проходом (чиста CREATE-міграція, без двозначності).

## Довідники підключення
- `DATABASE_URL` — власник схеми, для міграцій (`drizzle-kit generate`/`migrate`) і сіду. Обходить RLS.
- `APP_DATABASE_URL` — `app_user`, non-owner, для запитів застосунку (`src/server/db/client.ts`). RLS діє.
- `DEV_TENANT_ID` — `TODO(auth)`, тимчасове джерело `tenantId` до появи сесій.

## Команди
```
docker compose up -d       # підняти Postgres локально
npm run db:generate        # згенерувати SQL-міграцію зі схеми
npm run db:migrate         # накотити міграції
npm run db:setup-role      # створити/оновити non-owner роль app_user (ідемпотентно)
npm run db:seed            # засіяти dev-тенант + приклад товару, категорії, 30 кольорів (lib/mocks/*.ts)
npm run db:studio          # Drizzle Studio (перегляд/редагування даних)
```

## Перевірка ізоляції тенантів (виконано під час підключення)
`EXPLAIN ANALYZE` на `SELECT ... WHERE tenant_id = ? AND status = 'active'` — план використовує `Index Scan using products_tenant_status_idx`, без seq scan. Ізоляція перевірена і на рівні data-access шару (свій/чужий `tenantId` → товар/`null`), і напряму як `app_user` без жодного `WHERE tenant_id` у самому SQL (RLS сама відсікає чужі рядки) — див. розділ вище.

Для `categories` (write + read, окремо від products) перевірено так само: чужий тенант не бачить жодної категорії (`listCategories` → 0), не може прочитати категорію, створену іншим тенантом (`getCategoryById` → `null`), і `createCategory` під одним тенантом не робить дані видимими іншому.

Для зв'язку `products.category_id`: чужий тенант, знаючи `productId` і `categoryId`, не може змінити чужий товар (`updateProductCategory` під чужим `tenantId` — 0 рядків, значення лишається попереднім), і `getProductCountsByCategory` під чужим тенантом повертає порожній об'єкт, навіть коли у свого тенанта є прив'язані товари.
