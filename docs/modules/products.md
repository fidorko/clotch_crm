# Модуль: products

**Статус:** UI `в роботі` · Логіка `—` · Бек `—` · БД `—`
**Маршрут:** /products/[id]
**Оновлено:** 2026-07-29 (попередження ціни — тільки по Enter, стала висота рядка)

## Призначення
Картка товару: загальна інформація, фото, варіанти SKU (колір × розмір), залишки, статистика по товару. За зразком `Image.png`.

## Файли
| Шлях | Роль |
|---|---|
| src/app/products/layout.tsx | layout з сайдбаром для розділу товарів |
| src/app/products/[id]/page.tsx | сторінка картки товару, збирає секції |
| src/components/products/ProductHeader.tsx | хлібні крихти, заголовок, статус (єдине місце редагування — випадаючий список), дії |
| src/components/products/ProductTabs.tsx | список вкладок (`TabsList`/`TabsTrigger`); `Tabs`-корінь тепер у `page.tsx`, щоб `TabsContent` мав спільний контекст |
| src/components/products/ProductGeneralTab.tsx | клієнтська обгортка вкладки «Основне»: тримає стан цінової політики (`pricing`) — `page.tsx` лишається серверним (`generateMetadata`), тому стан не можна підняти вище нього; рахує грн-значення (не %) і пробрасовує їх у `ProductSkuSection` |
| src/components/products/ProductInfoPanel.tsx | ліва панель — характеристики товару, поля редагуються через випадаючий список; `pricing` тепер контрольований ззовні (`ProductGeneralTab`) |
| src/components/products/ProductPhotoGallery.tsx | фото моделі + мініатюри |
| src/components/products/ProductMetaPanel.tsx | права панель — метадані (редаговані: постачальник, країна бренду/виробник, внутрішній артикул, артикул постачальника, розміри/вага для ТТН, теги, опис) |
| src/components/products/ProductSkuSection.tsx | клієнтський компонент: таблиця SKU + деталі вибраного SKU; пробрасовує `product.measurements` і резолвнуту (грн) `pricing` (тип `SkuPricing`) у деталі SKU |
| src/components/products/ProductSkuTable.tsx | таблиця варіантів колір×розмір; у комірці — код, штрихкод, місце зберігання (`Комірка: A-01-01-01`); кнопка «Друк всіх SKU» в шапці картки |
| src/components/products/ProductSkuDetailPanel.tsx | панель деталей вибраного SKU: назва SKU й штрихкод редаговані (олівчик, штрихкод — ще й автогенерація), ціни дзеркалять `ProductInfoPanel` (лише грн, без %, кожна — окремий редагований override), кнопки дій з іконками + «Друк одного SKU» |
| src/components/products/SkuMeasurementsDialog.tsx | попап (`Dialog`) редагування замірів для конкретного SKU: тип (Combobox з пошуком) — мініатюра-візуалізація (заглушка, з БД пізніше) — значення (см) — видалити; сид зі `product.measurements`, далі незалежний локальний стан |
| src/components/products/ProductSizeChart.tsx | таблиця «Розмірна сітка» (NT/обхвати/INT/UA-RU/EU); вкладка «Розміри та заміри», ліва колонка ряду (3fr) |
| src/components/products/ProductMeasurements.tsx | картка «Заміри виробу» — рядки «тип заміру (Combobox — пошук по вводу) — значення (Input, см) — видалити»; згортається, «+ Додати ще один замір»; вкладка «Розміри та заміри», права колонка ряду (2fr) |
| src/components/products/ProductStatsBar.tsx | нижня панель підсумкових метрик |
| src/lib/types/product.ts | типи Product, ProductSku, ProductPhoto |
| src/lib/mocks/products.ts | мокові дані товару |
| src/lib/constants/product-status.ts | єдине джерело статусів товару (значення/лейбл/колір бейджа), використовує і хедер, і мета-панель |
| src/lib/constants/measurement-types.ts | спільний довідник типів замірів (`id`, `label`, `imageUrl`) — використовують і `ProductMeasurements`, і `SkuMeasurementsDialog` |
| src/components/dev/DevBlockLabel.tsx | dev-мітка блоку/секції (спільна, ui-kit.md) |
| src/lib/dev/dev-flags.ts | прапорець показу dev-міток по модулях |

## Використовує з ui-kit
Sidebar, Button, Badge (variant success/warning/secondary), Card, Tabs, Table, DropdownMenu (з DropdownMenuCheckboxItem), Tooltip, StatTile, DetailRow, Select, SelectRow, NumberRow, TextRow, PriceModeRow, Combobox, Dialog, Textarea, Input

## Дані
Зараз: моки в `src/lib/mocks/products.ts` (`product.measurements`, `sku.cell`). Розмірна сітка (`ProductSizeChart.tsx`) — окремо захардкоджена, уніфікована таблиця (не прив'язана до товару чи категорії). Типи замірів — спільний тимчасовий довідник `lib/constants/measurement-types.ts` (поле `imageUrl` поки завжди порожнє — плейсхолдер-іконка в UI).
Планово: таблиці `products`, `product_skus` (див. `db.md`) — ще не створені, БД не підключена. Розмірна сітка й типи замірів — окремі довідники, ймовірно з прив'язкою до категорії/типу одягу; `imageUrl` на кожен тип заміру — окреме зображення-візуалізація (що саме міряти), з БД.

## Доступ
Ролі, яким доступний модуль: owner (поки єдина активна роль).

## Зв'язки
Залежить від: layout (ui-kit, Sidebar)
Від нього залежать: —

## Зроблено
- 2026-07-29 — `PriceModeRow` (ui-kit): попередження «Ціна нижча за закупівельну» раніше пере-обчислювалося на кожне натискання клавіші (блимало під час набору, бо йшло напряму від live-стану) — тепер показується лише після Enter/blur (окремий `committedWarning` стан, синхронізується в момент коміту). Рядок попередження тепер рендериться завжди (порожній текст + `invisible`, а не умовний `{warning && <p>}`), тому висота рядка більше не стрибає при появі/зникненні повідомлення — однакова для всіх `PriceModeRow` незалежно від того, є попередження чи ні
- 2026-07-29 — `ProductSkuDetailPanel` — велике перероблення деталей SKU: (1) назва SKU у заголовку картки редагована через олівчик; (2) штрихкод (EAN) редагований + кнопка автогенерації (як була в старому `BarcodeRow`); (3) усі старі ціни SKU (закупівельна/роздрібна/оптова/акційна/мінімальна) прибрано, замість них — ті самі 6 полів, що в `ProductInfoPanel` (закупівельна/моя роздрібна/перечеркнута/моя оптова/моя дропшип/знижка роздрібна), кожне лише в грн (без перемикача %), редаговане через олівчик; значення приходять з `ProductGeneralTab` (новий клієнтський wrapper вкладки «Основне», тримає `pricing`-стан, бо `page.tsx` лишається серверним через `generateMetadata`) — `ProductInfoPanel` тепер контрольований ззовні; (4) «Вага SKU», «Об'єм SKU», «Постачальник», «Дата приходу» прибрано з панелі й з мок-об'єкта `selectedSku`; (5) кнопки «Історія руху»/«Перемістити» отримали іконки (`History`/`ArrowRightLeft`); (6) «Редагувати SKU» видалено; (7) додано «Друк одного SKU» (`Printer`) після кнопки «Заміри». У `ProductSkuTable` — кнопка «Друк всіх SKU» в шапці картки
- 2026-07-29 — два фікси в `ProductMeasurements` і `SkuMeasurementsDialog` (обидва мали той самий баг): «+ Додати ще один замір» більше не підставляє перший тип зі списку за замовчуванням — новий рядок стартує з порожнім типом; плейсхолдер комбобоксу — «Почніть вводити назву заміру» замість «Оберіть замір». У `SkuMeasurementsDialog`: мініатюра заміру (`size-12`) лишається компактною, але клікабельна — відкриває вкладений `Dialog` із великим переглядом (`aspect-[3/4]`, ширина `sm:max-w-sm`, як у головного фото товару `ProductPhotoGallery`); заголовок вкладеного попапу — назва типу заміру
- 2026-07-29 — `ProductSkuTable`: у комірці SKU замінено «Зал: N» на «Комірка: A-01-01-01» (місце зберігання; поле `sku.cell`, нове в типі `ProductSku`, замокано для всіх 18 SKU). Додано попап `SkuMeasurementsDialog` (`Dialog`, ui-kit) — кнопка «Заміри» (іконка лінійки) в рядку дій `ProductSkuDetailPanel`, поруч з «Історія руху»/«Перемістити». Той самий патерн рядка, що й `ProductMeasurements` (Combobox з пошуком + Input + видалити + додати), але кожен рядок тепер має мініатюру-візуалізацію заміру зліва (`size-12`, поки завжди плейсхолдер-іконка — `imageUrl` порожній, підтягуватиметься з БД пізніше, свій на кожен тип). Довідник типів замірів винесено в спільний `lib/constants/measurement-types.ts` (`id`, `label`, `imageUrl`) — використовують і `ProductMeasurements`, і новий попап. Попап сідиться з `product.measurements`, далі — незалежний локальний стан (не синхронізується назад із вкладкою «Розміри та заміри»)
- 2026-07-29 — під ціновою політикою `ProductInfoPanel` — рядок «Маржа роздріб / Маржа опт / Маржа дроп» (3 колонки, `MarginTile`): іконка `Percent` (один колір для всіх трьох, незалежно від значення) + число і в %, і в грн (`58.3% · 699.00 грн`), формула `(ціна продажу − закупівельна) / ціна продажу`. Якщо ціна (роздрібна/оптова/дропшип) виявляється нижчою за закупівельну — під відповідним рядком `PriceModeRow` червоним текстом «Ціна нижча за закупівельну» (новий проп `warning`), і сама маржа для цього каналу теж червона (від'ємна)
- 2026-07-29 — цінова політика в `ProductInfoPanel` повністю перероблена, новий порядок полів: «Закупівельна ціна» (тільки перегляд, `500.00 грн`, редагування — клік на олівчик; в майбутньому підтягуватиметься з поставки/БД, тому окремий компонент `PurchasePriceRow`, не `NumberRow`) → «Моя роздрібна ціна» → «Перечеркнута ціна» (як і раніше, фіксована сума) → «Моя оптова ціна» (нове поле) → «Моя ціна дропшипперам» (нове поле) → «Знижка роздрібна». Три «Моя ... ціна» поля і «Знижка роздрібна» — новий спільний `PriceModeRow` (ui-kit, 4 використання): перемикач «Грн»/«%» біля числового поля, підпис інпута (суфікс) міняється відповідно; в режимі `%` показує обчислене значення (`= X.XX грн`) — для трьох цінових полів це закупівельна ціна × (1 + %/100), для знижки — моя роздрібна ціна × (1 − %/100). Тип `Product.pricing` перероблено: `price`/`discountPercent`/`autoMarkup` прибрано, замість них — `retail`/`wholesale`/`dropship`/`retailDiscount` (кожне — новий тип `PriceModeValue { mode, amount, percent }`), `purchasePrice`/`oldPrice` лишились простими числами. Перемикач «Автоматичний розрахунок націнки» прибрано зовсім — його функцію тепер закриває режим `%` на кожному полі окремо, тримати обидва механізми одночасно було б суперечливо
- 2026-07-29 — вкладка «Варіанти (SKU)» видалена зі списку табів, замість неї — «Розміри та заміри» (`ProductSizeChart` + `ProductMeasurements`, той самий ряд `3fr_2fr`). Перша робоча логіка перемикання вкладок: `Tabs`-корінь перенесено з `ProductTabs.tsx` у `page.tsx` (охоплює і список табів, і вміст — `TabsContent` мусить бути в тому самому контексті), `ProductTabs` тепер рендерить лише `TabsList`. «Основне» — решта вмісту (статистика, InfoPanel/PhotoGallery/MetaPanel, SKU-секція), як і раніше. Решта вкладок (Залишки, Постачання...) поки без вмісту
- 2026-07-29 — `ProductMeasurements`: тип заміру тепер обирається через `Combobox` (новий `ui/combobox.tsx`, обгортка над `@base-ui/react/combobox` — той самий стиль, що й `Select`, але з полем пошуку: ввід одразу фільтрує список) замість звичайного `Select`; у кожного рядка додана кнопка видалення (іконка кошика, `Trash2`, `variant="ghost"`). `ProductStatsBar` перенесено з самого низу сторінки одразу під `ProductTabs` (був запит переставити панель метрик вище, ближче до заголовка)
- 2026-07-29 — розмірна сітка вийшла задуже широкою на всю ширину сторінки (розтягнута таблиця з великими проміжками). Винесено з `ProductSkuSection` в окремий рядок на сторінці (`page.tsx`), поруч додано новий блок `ProductMeasurements` («Заміри виробу») — обидва тепер у грід-рядку `3fr_2fr` (та сама пропорція, що й SKU-таблиця + деталі SKU вище), кожен зі своєю `DevBlockLabel`. `ProductMeasurements`: рядки «тип заміру (`Select`, тимчасовий довідник) — значення в см (`Input`)», картка згортається (шеврон, локальний стан), «+ Додати ще один замір» додає новий рядок з дефолтним типом. Новий тип `ProductMeasurement` (`id`, `type`, `valueCm`), поле `product.measurements`. Зразок — скрін користувача, але без банерного заголовка й зебра-смуг оригіналу — рядки на `divide-y`, як і решта карток-панелей проєкту
- 2026-07-29 — додано `ProductSizeChart` — таблиця «Розмірна сітка» (NT-розмір, обхвати грудей/талії/стегон, INT джинси, UA/RU, EU), 9 рядків XS–5XL, під SKU-секцією (`ProductSkuSection`). За зразком-скріном користувача, але без кольорової палітри оригіналу — той самий нейтральний `Table`/`Card`, що й `ProductSkuTable`, щоб не ламати єдиний стиль

## Відкрито
- [ ] ціни в `ProductSkuDetailPanel` — сідяться з `ProductInfoPanel` (спільний `pricing` через `ProductGeneralTab`), але кожна одразу стає незалежним override, не прив'язаним до конкретного `sku.id` — при перемиканні між SKU в таблиці override не скидається; реальні per-SKU ціни зберігати в `product_skus`, коли підключиться БД
- [ ] реальне збереження фото — зараз лише локальний прев'ю (`blob:`, зникає після перезавантаження сторінки); треба файлове сховище поза webroot + таблиця `product_photos` (`tenant_id`) при підключенні БД
- [ ] логіка вкладок — наповнені «Основне» й «Розміри та заміри», решта (Залишки, Постачання, Продажі, Фото, Опис, Файли, Історія) поки без вмісту
- [ ] підключення БД замість моків
- [ ] список товарів (сторінка `/products` без id)
- [ ] `generateMetadata` — замінити `mockProduct.name` на реальний title за `id`, коли з'явиться fetch з БД
- [ ] вимкнути `DEV_BLOCK_LABELS.products` (`lib/dev/dev-flags.ts`), коли статус UI стане `готово`
