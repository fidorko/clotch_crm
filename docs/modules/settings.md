# Модуль: settings

**Статус:** UI `в роботі` · Логіка `—` · Бек `в роботі` · БД `в роботі`
**Маршрут:** /settings?tab=..., /settings/categories/new, /settings/categories/[id], /settings/references/colors, /settings/references/suppliers, /settings/references/suppliers/new, /settings/references/suppliers/[id]
**Оновлено:** 2026-07-30 (довідник «Постачальники» — другий пункт «Довідників» із реальною БД)

## Призначення
Сторінка налаштувань магазину: розділи (вітрина, загальні, категорії товару, замовлення, довідники, склади, доставка, оплата, тарифний план) — вибираються через підменю сайдбару (розгортається інлайн під «Налаштування», решта пунктів сайдбару лишається видимою), розділ у URL як `?tab=`. «Категорії товару» — повний CRUD з реальною БД (список/створення/редагування/видалення, ієрархія, зображення). «Довідники» — список-навігація на 14 пунктів; «Кольори» й «Постачальники» — з реальною сторінкою й БД, решта 12 ще без даних.

## Файли
| Шлях | Роль |
|---|---|
| src/app/settings/layout.tsx | лише `robots`-метадані розділу — сайдбар тепер глобальний, у кореневому `src/app/layout.tsx` (ui-kit.md) |
| src/app/settings/page.tsx | сторінка, збирає header + активний розділ; розділ читається з `searchParams.tab` (не Tabs-стан), невідоме/відсутнє значення тихо трактується як `"categories"` (URL-параметр не повинен ламати сторінку); дані (`listCategories`+`getProductCountsByCategory` або `listColors`) тягнуться лише під активний розділ, не всі одразу; кожен блок обгорнутий `DevBlockLabel` (`dev = DEV_BLOCK_LABELS.settings`) |
| src/components/settings/SettingsHeader.tsx | хлібна крихта (одна, «Налаштування» — сторінка верхнього рівня, не вкладена, тепер у рядку разом з `HeaderActions` праворуч) + заголовок |
| src/components/settings/CategoriesTab.tsx | вкладка «Категорії товару» (Client Component) — `categories: CategoryRow[]` і `productCounts: Record<string, number>` пропами із сервера (без локального стану даних, тільки UI-стан). Усе в одній білій `Card` (як `ProductInfoPanel`): пошук + фільтр статусу (реально фільтрують), масове виділення (виділення батьківської категорії каскадно виділяє всіх нащадків — `getDescendantIds`), дворівнева ієрархія (`buildCategoryTree` зі `lib/categories/tree.ts`); під час пошуку ієрархія ховається — плаский список збігів. «Товарів» — реальна кількість (`effectiveProductCount`: пряма з `productCounts` + рекурсивна сума дітей для батьківської). Назва категорії — `Link` на `/settings/categories/[id]` (клікабельна, веде в редагування). Дії по рядку: око (`Eye`/`EyeOff`) → `toggleCategoryActiveAction`, олівець → `Link` на `/settings/categories/[id]`, кошик → `DeleteCategoryButton` (підтвердження `Dialog`) → `deleteCategoryAction`. «Додати категорію» → `Link` на `/settings/categories/new`. Масове «Видалити» → `DeleteSelectedButton` (підтвердження, показує кількість) → `deleteCategoriesAction` — видаляє вибране «знизу вгору» (найглибші першими), тож батьківську й дітей можна видалити разом в один клік; часткові невдачі (категорія лишилась із дочірньою поза вибором) показуються окремим повідомленням, не валять весь запит |
| src/components/settings/CategoryForm.tsx | спільна форма створення/редагування (Client Component), проп `category: CategoryRow \| null` вирішує режим. Ліва колонка: картка «Основне» (назва, батьківська категорія — `CategoryTreeSelect`, виключає саму категорію й власних нащадків; опис) + картка «SEO» (H1/meta title/meta description зі лічильниками символів). Права колонка: одна картка — зображення (завантаження, реальний аплоад), 3 перемикачі (`Switch`: активна на вітрині / показувати в розділі магазину / показувати в меню шапки), вага/розміри за замовчуванням, кнопки «Створити категорію»/«Зберегти» + «Скасувати». Без перемикача «Показ опису» (свідомо не робили, за прямою вказівкою) |
| src/components/categories/CategoryTreeSelect.tsx | спільний ієрархічний `Select`-піклер категорій (з відступами за глибиною) — винесено з `CategoryForm`, коли той самий патерн знадобився вдруге в `ProductInfoPanel` (правило 9.2 CLAUDE.md); приймає `excludeIds` (виключити категорію й нащадків — для батьківського піклера) і опційний `noneOption` (для «— Коренева —») |
| src/app/settings/categories/new/page.tsx, [id]/page.tsx | Server Components — тягнуть `listCategories`/`getCategoryById` (тенант-скоуповано), рендерять `CategoryForm`; `[id]` — `notFound()`, якщо категорії нема під цим тенантом |
| src/app/settings/categories/actions.ts | Server Actions: `createCategoryAction`, `updateCategoryAction` (+ захист від циклу в ієрархії, `isDescendantCategory`), `deleteCategoryAction`, `deleteCategoriesAction` (масове — сортує `categoryDepth` спаданням, глибші видаляються першими), `toggleCategoryActiveAction`, `uploadCategoryImageAction`. Валідація на сервері (назва обов'язкова), `revalidatePath("/settings")` після кожної зміни |
| src/server/data/categories.ts | тенант-скоупований data-access шар: `listCategories`, `getCategoryById`, `createCategory`, `updateCategory`, `deleteCategory` (ловить FK-порушення `error.cause.code === '23503'` → дружній текст), `toggleCategoryActive`, `getProductCountsByCategory` (`GROUP BY category_id`, повертає `Record`, не `Map` — Map не серіалізується через RSC-проп) |
| src/server/storage/category-images.ts, src/app/api/uploads/categories/[filename]/route.ts | реальне файлове сховище (не blob-прев'ю) — диск поза webroot, per-tenant теки, валідація типу/розміру, роздача через route handler із `tenantId` із сесії (не з URL). Деталі — `db.md` |
| src/lib/categories/tree.ts | спільна утиліта дерева категорій (`buildCategoryTree`, `isDescendantCategory`, `getDescendantIds`, `categoryDepth`) — використовують `CategoriesTab`, `CategoryForm`, `CategoryTreeSelect`, `actions.ts` |
| src/components/settings/ReferencesList.tsx | розділ «Довідники» (`?tab=references`) — сітка плиток (`grid-cols-3` на `lg`, 2/1 на менших), кожна плитка: іконка в кольоровому колі (своя пастельна пара `bg/text` на 14 пунктів), назва, опис, лічильник, шеврон; під заголовком — прев'ю наявних значень (чіпи, максимум 7, далі «…», якщо більше). Проп `colors` (з `listColors`, `page.tsx`) — єдиний пункт із реальними значеннями (чіп = кольоровий гурток + назва); решта 13 без пропу `values`/`count` → плитка без прев'ю й без лічильника (чесно, не вигадані нулі). «Кольори» веде на `/settings/references/colors`, решта 13 ще `href="#"` |
| src/app/settings/references/colors/page.tsx | сторінка довідника кольорів — тягне `listColors(tenantId)`, рендерить `ColorsList`, «Назад до довідників» на `/settings` |
| src/app/settings/references/colors/actions.ts | Server Actions: `createColorAction`, `updateColorAction`, `deleteColorAction` — валідація на сервері (назва обов'язкова, hex — regex `^#[0-9a-fA-F]{6}$`), тенант-скоуповано через `server/data/colors.ts`, `revalidatePath("/settings/references/colors")` |
| src/server/data/colors.ts | тенант-скоупований data-access шар: `listColors`, `createColor`, `updateColor` (ловить `UNIQUE(tenant_id, name)`, код `23505`, → «Колір з такою назвою вже існує»), `deleteColor` |
| src/components/settings/ColorsList.tsx | Client Component: «Додати колір» створює рядок одразу (плейсхолдер «Новий колір» / `#CCCCCC`, редагується на місці — той самий патерн quick-create, що й «Додати товар» у products); кожен рядок — `<input type="color">` (квадратик-зразок, клік відкриває нативний спектр браузера, стилізований без webkit/moz внутрішньої рамки), текстове поле з HEX-кодом (ручний ввід, синхронізовано зі зразком в обидва боки) і поле назви — обидва зберігаються одразу по blur/Enter (`updateColorAction`); кошик → підтвердження `Dialog` → `deleteColorAction` |
| src/lib/mocks/colors.ts | 30 найпоширеніших кольорів одягу, назви англійською — джерело для `db:seed` |
| src/app/settings/references/suppliers/page.tsx | список постачальників — тягне `listSuppliers(tenantId)`, рендерить `SuppliersHeader` + `SuppliersList` |
| src/app/settings/references/suppliers/new/page.tsx, [id]/page.tsx | Server Components — `new` рендерить `SupplierForm` без даних, `[id]` тягне `getSupplierById` (`notFound()`, якщо нема під цим тенантом); заголовок/хлібні крихти — усередині `SupplierForm`, не в `page.tsx` (кнопка «Зберегти» прив'язана до стану форми) |
| src/app/settings/references/suppliers/actions.ts | Server Actions: `createSupplierAction`, `updateSupplierAction`, `deleteSupplierAction` — валідація й trim на сервері (назва обов'язкова; порожні контакти/канали/поля відфільтровуються перед збереженням), `revalidatePath` |
| src/server/data/suppliers.ts | тенант-скоупований data-access шар: `listSuppliers`, `getSupplierById` (супровідні контакти/канали/поля трьома паралельними запитами), `createSupplier` (генерує `code`, ретраїть до 5 разів на `23505`), `updateSupplier` (delete-then-insert усіх трьох дочірніх колекцій — `syncProductTags`-патерн), `deleteSupplier` (діти йдуть каскадом) |
| src/components/settings/SuppliersHeader.tsx | заголовок списку постачальників — той самий каркас, що `ProductsHeader`/`SettingsHeader`: клікабельні хлібні крихти + `HeaderActions` праворуч у першому рядку, назва + лічильник + «Додати постачальника» у другому |
| src/components/settings/SuppliersList.tsx | Client Component: таблиця-список (назва+код, тип, сайт, статус активності, кошик з підтвердженням `Dialog`) |
| src/components/settings/SupplierForm.tsx | спільна форма створення/редагування (Client Component), проп `detail: SupplierDetail \| null`. Сам рендерить заголовок (той самий каркас `ProductsHeader`: крихти + `HeaderActions` зверху, назва/код + «Скасувати»/«Зберегти постачальника» — кнопка збереження зовні `<form>`, зв'язана через HTML-атрибут `form="supplier-form"`, а не проп-дрилінг). Нижче — картки з іконкою перед заголовком (`SectionTitle`, той самий патерн, що плитки `ReferencesList`): «Основна інформація» (`grid-cols-2`: назва+код, тип+веб-сайт; перемикач активності на всю ширину нижче), «Контактна інформація» (`SupplierContactsFields`, нижче — адреса `grid-cols-3`: країна/місто/адреса, без індексу + `SupplierCustomFields`), «Месенджери та соціальні мережі» — окрема картка, `grid-cols-2` (ліва колонка `SupplierChannelFields` месенджери, права — соцмережі), «Додаткова інформація» (примітки, лічильник символів). Права колонка: `SupplierTypeLegend` |
| src/components/settings/SupplierIconInput.tsx | `Input` з іконкою-префіксом зліва (той самий патерн, що пошук у `CategoriesTab`, винесений — використано в `SupplierForm` (веб-сайт) і `SupplierContactsFields` (телефон/email)) |
| src/components/settings/SupplierContactsFields.tsx | масив контактних осіб — «Контактна особа N» + кошик у шапці блоку (не в рядку полів — щоб ширини не «сходинками», ловили це як зауваження), 4 поля (ім'я/посада/телефон/email) в єдиному `grid-cols-2` для однакової ширини всіх полів |
| src/components/settings/SupplierChannelFields.tsx | фіксований (не user-addable) список рядків «кружечок брендового кольору з фірмовою іконкою (`react-icons/fa6`) + назва + поле вводу» — використано двічі в `SupplierForm`, тепер поряд у `grid-cols-2` (месенджери зліва з `type="tel"`, соцмережі справа з `type="text"`), не одне під одним |
| src/components/settings/SupplierCustomFields.tsx | довільні пари «назва поля — значення» під блоком адреси (усередині «Контактна інформація»), додати/видалити рядок |
| src/components/settings/SupplierTypeLegend.tsx | статична права колонка: картка «Підказка» (`SupplierHintIllustration` + текст за зразком-скріном людини) і картка-легенда 5 типів постачальника (іконка + опис) — довідка до `Select` у формі |
| src/components/settings/SupplierHintIllustration.tsx | декоративна inline-SVG ілюстрація (людина з планшетом біля чек-листа) для картки «Підказка» — у проєкті немає сховища зображень для таких декоративних асетів, тому вручну намальована геометрична SVG, не растрове зображення |
| src/lib/constants/supplier-options.ts | `SUPPLIER_TYPE_OPTIONS` (5 типів, іконка+колір+опис), `MESSENGER_CHANNELS`/`SOCIAL_CHANNELS` (фірмова іконка `react-icons/fa6` + брендовий колір — `decisions.md`) |
| src/lib/types/supplier.ts | `SupplierFormInput`/`SupplierContactInput`/`SupplierChannelInput`/`SupplierCustomFieldInput` — форма передається в Server Action структурованим об'єктом напряму (не `FormData`, патерн `saveProductAction`), бо вкладені масиви незручно серіалізувати у `FormData` |
| src/lib/mocks/categories.ts | джерело для `db:seed` (не для UI — той тепер читає БД): ієрархія 3 батьківські групи (Одяг, Сукні та боді, Аксесуари) + окремий «Архів одягу»; групування ілюстративне, не звірене з реальною таксономією |
| src/components/ui/checkbox.tsx | базовий компонент (shadcn CLI), перше використання — масове виділення в `CategoriesTab` |

## Використовує з ui-kit
Sidebar (другорядне меню — розділи), HeaderActions, Card, Table, Select, Input, Button, Badge, Checkbox, Dialog, Switch, Textarea

## Дані
«Категорії товару» — таблиця `categories` у PostgreSQL (повний CRUD через `server/data/categories.ts` + Server Actions), тенант-скоуповано, RLS. `products.category_id` (FK на `categories.id`, `ON DELETE SET NULL`) — реальний зв'язок товару з категорією, звідки рахується «Товарів» у списку. Довідники — статичний масив у `ReferencesList.tsx` (14 пунктів, тепер плитками в сітці); «Кольори» (таблиця `colors`) і «Постачальники» (таблиці `suppliers`+3 дочірні) — з реальною БД, тенант-скоуповано, RLS; `page.tsx` тягне `listColors`/`listSuppliers` і пробрасовує пропами в `ReferencesList` для лічильника й прев'ю значень; 12 решти — без переходу нікуди, без БД, без лічильника/прев'ю. Деталі схеми — `db.md`.
Картка товару (`/products/[id]`) тепер читає `listColors(tenantId)` і пробрасовує в `ProductGeneralTab` → `ProductInfoPanel` (поле «Колір» без варіацій) та `ProductSkuSection` → `ProductSkuTable` (конструктор SKU-матриці, дропдаун «+Колір») — раніше обидва брали хардкод `COLOR_OPTIONS` з `lib/constants/sku-variant-options.ts` (тепер видалено, лишився тільки тип `ColorOption`).

**Нюанс Select з ui-kit:** якщо `value` опції не збігається з людським лейблом (напр. `"all"`/`"active"`, а не сам текст), `SelectValue` треба явно дати render-функцію (`<SelectValue>{(value) => label}</SelectValue>`) — інакше показує сирий `value`. У `CategorySelectRow` (`ProductInfoPanel`) цього не було видно, бо там `value` і лейбл — той самий рядок.

## Доступ
Ролі, яким доступний модуль: owner (поки єдина активна роль).

## Зв'язки
Залежить від: layout (ui-kit, Sidebar) — «Налаштування» в сайдбарі розгортає інлайн-підменю з розділами цього модуля (`?tab=...`), замість колишніх горизонтальних вкладок (`SettingsTabs`, видалено)
Від нього залежать: —

## Зроблено
- 2026-07-30 — довідник «Постачальники»: другий раунд правок за зауваженням («2 колонки», «різна ширина інпутів», «не зробив підказки»). (1) «Основна інформація» — `grid-cols-2` замість одноколонкового стеку; (2) «Адреса» перенесена в «Контактна інформація» (`grid-cols-3`: країна/місто/адреса — поле «Індекс» прибрано взагалі, разом з колонкою `postal_code` у БД, міграція `0009`); (3) «Месенджери»/«Соцмережі» винесено в окрему картку, поряд у `grid-cols-2` (раніше — одне під одним усередині «Контактної інформації»); (4) `SupplierContactsFields` — кошик видалення переїхав із рядка полів у шапку блоку («Контактна особа N»), усі 4 поля контакту тепер в єдиному `grid-cols-2` (раніше рядок телефон/email без кнопки поруч мав іншу ширину, ніж рядок ім'я/посада з кнопкою — «сходинка»); (5) картка «Підказка» отримала намальовану SVG-ілюстрацію (`SupplierHintIllustration`) замість голої іконки — за зразком-скріном людини
- 2026-07-30 — довідник «Постачальники» (`/settings/references/suppliers`) допрацьовано за зауваженням («не схоже на зображення яке я надавав» + «іконки повинні бути фірмовими»): (1) заголовок форми/списку тепер той самий каркас, що `ProductsHeader` — клікабельні крихти + `HeaderActions` зверху, назва/лічильник/дії в другому рядку (раніше — голий `h1` без цього каркасу); кнопка «Зберегти» — зовні `<form>`, зв'язана `form="supplier-form"`; (2) іконка в кольоровому колі перед заголовком кожної картки (`SectionTitle`) і префікс-іконки в полях Веб-сайт/Телефон/Email (`SupplierIconInput`) — той самий візуальний патерн, що плитки `ReferencesList`/пошук `CategoriesTab`; (3) іконки месенджерів/соцмереж замінено з 2-літерних кольорових бейджів на справжні фірмові лого — додано `react-icons` (`fa6`), погоджено з людиною через питання (`decisions.md`, правило 9.9 CLAUDE.md)
- 2026-07-30 — довідник «Постачальники» (`/settings/references/suppliers`), перший прохід: нові таблиці `suppliers` (`code` автогенерується, `SUP-0001`...) + `supplier_contacts`/`supplier_channels`/`supplier_custom_fields` (усі — delete-then-insert при збереженні, `db.md`). Форма за зразком-скріном людини з уточненнями: країна лише в блоці адреси (без дублювання), тип постачальника — `Select` з легендою праворуч (картку «Статус співпраці» прибрано), веб-сайт перенесено у верхній блок, окреме поле телефону на кожен месенджер (Telegram/Viber/WhatsApp) + соцмережі (Facebook/Instagram/TikTok/Threads), довільні поля в адресі, кілька контактних осіб. Перевірено: `tsc`/`eslint` чисто, прямий тест data-шару (create/get/update/delete, генерація коду з ретраєм на колізію, delete-then-insert дітей) і рендер усіх трьох сторінок — без помилок
- 2026-07-30 — сайдбар виправлено за зауваженням: (1) `h-screen` замість `min-h-screen` на кореневій обгортці (`layout.tsx`) — сайдбар тепер завжди на всю висоту екрана, фіксований, скролиться лише `<main>`; (2) «Налаштування» розгортає підменю **інлайн** одразу під собою, решта основних пунктів (Товари, Замовлення...) лишається видимою — попередній варіант (drill-down: підміна всього списку навігації, з «← Назад») прибрано; (3) виправлено React-помилку «two children with the same key, `#`» — ключ у `.map()` був `item.href`, а кілька ще не збудованих пунктів (`href="#"`) не унікальні; тепер ключ — `item.label`
- 2026-07-30 — навігація розділів налаштувань перенесена з горизонтальних вкладок (`SettingsTabs`, видалено) у сайдбар (деталі — `ui-kit.md`, «Sidebar»). `page.tsx` більше не тримає `Tabs`-стан — читає розділ з `searchParams.tab` (Server Component), невідоме/відсутнє значення тихо стає `"categories"`; дані під кожен розділ (категорії/кольори) тягнуться лише коли розділ активний, а не всі одразу наперед. Заразом сайдбар став глобальним (кореневий `src/app/layout.tsx`), прибрано дублювання обгортки в `products/layout.tsx` й `settings/layout.tsx`
- 2026-07-30 — `ReferencesList` перероблено з одноколонкового списку (`divide-y`) на сітку плиток (за зразком-скріном людини), 3 колонки на `lg`: іконка в кольоровому колі + назва + опис + лічильник + шеврон; під заголовком — прев'ю наявних значень плитки (чіпи, максимум 7, далі «…»). «Кольори» — єдиний пункт із реальними даними (`listColors`, тепер пропом із `page.tsx`, замість статичного масиву без БД) — чіпи показують кольоровий гурток + назву; решта 13 пунктів без лічильника й без чіпів (чесно — там немає БД, а не вигаданий нуль)
- 2026-07-30 — довідник «Кольори» (`/settings/references/colors`, перший реальний пункт «Довідників»): нова таблиця `colors` (`tenant_id`, `name`, `hex`, `is_active`, `position`, RLS, UNIQUE `(tenant_id, name)` — `db.md`), тенант-скоупований data-access шар + Server Actions (create/update/delete, дружня помилка на дублікат назви — код `23505`). `ColorsList`: «Додати колір» створює рядок одразу (плейсхолдер, редагується на місці), кожен рядок — квадратик-зразок (`<input type="color">`, клік відкриває нативний спектр браузера) + текстове поле HEX (ручний ввід, синхронізовано зі зразком в обидва боки) + поле назви, усе зберігається одразу по blur/Enter. Dev-сід заповнює 30 найпоширеніших кольорів одягу англійською (`lib/mocks/colors.ts`). Картка товару (`ProductInfoPanel`, `ProductSkuTable`) тепер бере кольори з цього довідника замість хардкоду `COLOR_OPTIONS` (видалено з `lib/constants/sku-variant-options.ts`)
- 2026-07-30 — реальний зв'язок товар↔категорія: `products.category_id` (FK, `SET NULL`), «Товарів» у списку категорій тепер справжня кількість (`getProductCountsByCategory`, рекурсивна сума для батьківських — `effectiveProductCount`). Категорія товару в `ProductInfoPanel` тепер зберігається в БД (раніше — лише локальний React-стан, перше поле в цій панелі, що персистить). Спільний піклер `CategoryTreeSelect` винесено з `CategoryForm` для повторного використання. Назва категорії в таблиці — клікабельна, веде на редагування
- 2026-07-30 — «Категорії товару»: масове видалення тепер справді працює, включно з випадком «вибрав батьківську й дітей разом» (раніше FK `ON DELETE RESTRICT` заблокував би це, якщо видаляти в довільному порядку — `deleteCategoriesAction` сортує вибране за глибиною й видаляє найглибші першими). Виділення батьківської категорії каскадно виділяє всіх нащадків у таблиці (`getDescendantIds`). Перевірено в браузері: вибрав категорію з дитиною, обидва чекбокси відмітились, масове видалення прибрало обидві одним запитом (12 → 10 рядків у БД)
- 2026-07-30 — «Категорії товару» підключено до PostgreSQL (окрема гілка `feat/categories-crud`, rollback-точка `47b13d5`): нова таблиця `categories` (самопосилання `parent_id` з `ON DELETE RESTRICT`, RLS, індекси — `db.md`), тенант-скоупований data-access шар + Server Actions, повний CRUD (список читає БД, створення/редагування — спільна `CategoryForm` на `/settings/categories/new` і `/[id]`, видалення блокується на рівні БД, якщо є діти — з дружнім повідомленням). Реальне файлове сховище для зображень категорій (перше в проєкті, не blob-прев'ю — окреме рішення від фото товару, `decisions.md`). Перевірено: create/edit/delete/upload у браузері, ізоляція тенантів (read+write) окремо для `categories`, захист від циклу в ієрархії при зміні батьківської категорії. Дорогою знайдено й виправлено баг: Drizzle ховає код помилки Postgres у `error.cause.code`, не `error.code` — без фіксу користувач бачив сирий SQL замість «Спочатку видаліть дочірні категорії»
- 2026-07-30 — «Категорії товару»: кнопка-око (`Eye`/`EyeOff`) у діях реально перемикає видимість категорії (`Активна`/`Прихована` — перейменовано з «Неактивна» для точності), єдина дія в цій вкладці, яка справді щось змінює (локальний стан)

## Відкрито
- [ ] 7 із 9 розділів без вмісту (Вітрина магазину, Загальні, Замовлення, Склади, Доставка, Оплата, Тарифний план) — плейсхолдер-текст «ще в розробці»
- [ ] «Обліковий запис»/«Вихід» у футері сайдбару — поки `href="#"`, немає авторизації (`TODO(auth)`); немає сторінки облікового запису й механізму виходу
- [ ] Основна навігація сайдбару (Головна, Замовлення, Клієнти, Постачальники, Склад, Аналітика, Маркетинг) — здебільшого `href="#"`, самі розділи ще не збудовані
- [ ] вибір категорії товару оновлює лише `products.category_id` — старі текстові поля `category`/`category_path` (хлібні крихти в `ProductHeader` тощо) НЕ синхронізуються автоматично, можуть розійтися з реально обраною категорією (`db.md`)
- [ ] drag-and-drop сортування (`position` у БД уже є, `GripVertical` у UI) — досі лише візуальний, перетягування не реалізоване
- [ ] «Переглянути» (іконка зовнішнього посилання) на рядку категорії — декоративна, нема публічної вітрини, куди вести
- [ ] `tenant_id` для категорій і для зображень — тимчасово з dev-константи (`TODO(auth)`, як і для товарів)
- [ ] 12 із 14 пунктів «Довідники» ведуть в нікуди (`href="#"`) — самі сторінки довідників (Колекції, Сезон тощо), крім «Кольори» й «Постачальники», ще не зроблені, без БД
- [ ] «Кольори» — немає перемикача `is_active`/сортування (`position` у БД є, drag-and-drop не реалізовано, як і в «Категоріях товару»); дубльована назва серед seed-кольорів іншого тенанта не конфліктує (UNIQUE у межах тенанта), але сам список 30 кольорів ілюстративний, не звірений з реальним асортиментом
