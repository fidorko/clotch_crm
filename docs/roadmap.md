# Roadmap

Росте знизу, по мірі надходження задач. Пункт стає `[x]` тільки після підтвердження людини (крок 4, CLAUDE.md).

## В роботі

- [ ] Розділ «Доставка» (`settings` → `?tab=delivery`, сайдбар «Доставка» тепер веде сюди, був плейсхолдер «ще в розробці») — 4 проходи, пряма вказівка людини. **Перший** — список способів доставки (Самовивіз/Нова Пошта/Укрпошта/Meest Express — засіяно, можна додати довільний), у кожного вмикач, олівець-редагування, кошик-видалення, статус (Активний/Не активний/Потрібен ключ), реальна БД (`delivery_methods`), CRUD. Людина дала тимчасовий реальний ключ Нової пошти в чаті — лише в `.env` (`NP_API_KEY`, людина дописала вручну — пісочниця блокує запис/читання `.env` навіть після дозволу), сід підставляє його в дев-тенант, живим викликом `Address.getCities` підтверджено робочий. Референс — `github.com/shopanaio/carrier-api` (TS-клієнт + дзеркало документації НП) використано лише для пошуку методів API, БЕЗ npm-залежності — код проти `api.novaposhta.ua` написано самостійно. **Другий** — попап редагування розширено повним набором CRM-налаштувань перевізника за детальним списком людини («не всі вони є в API, але потрібні»): Дані API + реальне «Перевірити підключення» (`Address.getCities` з уведеним ключем, лише для НП), Відправник за замовчуванням, Тип доставки можливий, Хто платить, Контроль оголошеної вартості, Автоматичні дії (частота синхронізації, авто-повернення при відмові на відділенні, repeatable-список «статус перевізника → статус замовлення» через реальний `order_statuses`), Пакування, Друк — нова таблиця `delivery_method_status_rules` + ~17 нових колонок `delivery_methods` (міграції `0052`/`0053`). **Третій** — «Опис відправлення» (що в поле «Опис вантажу» ЕН: ID замовлення/артикул/назви товарів + кількість). **Четвертий** («дані відправника мають тягнутися з API з мого ключа, довідник міст/відділень теж підтягнути, все у формі перевір по документації API») — дослідив живими викликами реальний API (виявив правильну модель `CommonGeneral`, не `Reference`, для довідників): відправник (контрагент/контактна особа+телефон/місто/відділення) тепер реальні Select/пошуковий Combobox з ключа тенанта (`Counterparty`/`Address` API), «Тип доставки можливий» замінено з 3 вигаданих прапорців на реальні 5 комбінацій `getServiceTypes`, «Хто платить» підтверджено живим викликом (хедж «якщо підтримується» прибрано), «Пакування перевізника» відкриває живий пошук над реальним `getPackList`. «Автоматичне повернення» перейменовано на «оформлювати повернення через API» — людина уточнила: автоматики нема, лише можливість замовити повернення по API (сам виклик ще не реалізовано). Для перевізників без реальної інтеграції (не Нова пошта) — фолбек на вільний текст, як було. Дві міграції (`0054` додавання нових колонок, `0055` видалення старих 3 булевих) розділено, щоб уникнути ambiguous-rename prompt drizzle-kit (недоступний TTY в сесії). Усе нове — лише зберігання конфігурації плюс самі живі довідникові пошуки; жодного фонового job/реального створення ЕН/реального виклику повернення не реалізовано. `tsc`/`next build`/`eslint` чисто, живою перевіркою Playwright (усі секції попапу, реальний тест ключа, каскад контрагент→контактна особа з реальними даними акаунту, живий пошук міста/відділення, повне заповнення+збереження+читання з БД) і прямим SQL підтверджено — тестові дані (включно з реальними персональними ім'ям/телефоном акаунту) видалено з БД одразу після перевірки — чекає звірки з людиною, див. `modules/settings-delivery.md`, `db.md`

- [ ] Довідник «Статуси замовлень» (settings → Довідники → Системні) — реальна БД (`order_statuses`, міграція `0049`), пряма вказівка людини «наперед», поки сам модуль «Замовлення» ще на моках. Назва+колір (той самий UX, що «Кольори») + опційне сповіщення на кожному статусі («через N год без зміни → користувач», поки 1 опція `DEV_USER.name`, `TODO(auth)` — сама перевірка/розсилка не реалізована, лише конфігурація). Дев-сід — 11 стартових статусів за прямим списком людини (Прийнято→Комплектується→Відправлено→На відділенні 1Д…7Д→Повернення). `tsc`/`eslint` чисто, живою перевіркою headless Playwright + прямим SQL підтверджено: створення/редагування/сповіщення-конфіг переживає перезавантаження/видалення — усі пройшли (по дорозі виправлено ту саму Select controlled/uncontrolled помилку, що раніше в `warehouse-receiving`). Довідник поки не пов'язаний з `lib/types/orders.ts` (там власний хардкод-enum) — чекає звірки з людиною, див. `settings-references.md`, `db.md`

- [ ] Список замовлень (`/orders`, сайдбар «Замовлення» тепер веде сюди, був `href="#"`) — сучасний CRM-патерн, продумано самостійно (пряма вказівка людини «продумай сам, за зразком сучасних CRM»): 5 KPI-плиток, фільтри (пошук/дата/статус/оплата/канал), таблиця (чекбокс, кольорова іконка каналу, клієнт з аватаром-літерою, товари, сума+спосіб оплати, бейдж оплати, доставка+ТТН+склад, бейдж статусу пайплайна з 7 станів, менеджер, дії-заглушки), клієнтська пагінація з вибором «Відображати по» (50/100/200/500/1000). **Другий прохід (2026-08-05)** — масове виділення (чекбокси + ctrl/cmd-клік по рядку) з bulk-тулбаром у шапці: «Змінити статус» (реально мутує мок-стан, скидається на F5) + «Експорт» (реальний CSV у браузері, без беку) + «Створити замовлення» (чесна заглушка, форми ще нема). **Третій–четвертий проходи** — «Друк ТТН» у тому самому тулбарі: попап з великими квадратними кнопками перевізників (Нова пошта/Укрпошта, наближена SVG-реконструкція реальних лого, кількість обраних замовлень усередині) + чекбокси «Дія після друку» (змінити статус/повідомити — незалежні, можна обидва; «нічого не робити» блокує обидва) — реального друку/API перевізника нема, лише застосування дії до мок-стану; попап закривається лише хрестиком (клік по фону/Escape ігноруються), підключення способів доставки й реального маркування по API — майбутня задача. **Мок-перший підхід** (людина обрала — у БД ще нема ні `orders`, ні `customers`) — 54 детерміновані записи (`lib/mocks/orders.ts`, свій seeded PRNG), склад відвантаження — реальні назви з `warehouses` тенанта. `tsc`/`eslint` чисто, live-перевірка headless Playwright (рендер/пагінація/пошук/фільтри/масове виділення/bulk-дії/друк ТТН/консоль — усі пройшли) — чекає візуальної звірки з людиною, див. `modules/orders.md`
- [ ] Модуль товарів (products) — картка товару за зразком `Image.png`. UI зверстано на моках, чекає звірки з людиною.
- [ ] Сторінка складу (warehouse, `/warehouse`) — сайдбар «Склад» тепер веде сюди (був `href="#"`). Плитки складів за зразком-скріном людини (другий прохід — уточнена деталізація: SVG-ілюстрація складу, вертикальні кольорові показники, кольорові чіпи операцій). Назва/адреса/статус «Закритий» — реальні з `warehouses`, решта — мок (`lib/mocks/warehouse-cards.ts`), 5 із 6 кнопок операцій — заглушки (бекенду ще нема). UI зверстано, чекає звірки з людиною — див. `modules/warehouse.md`
- [ ] Надходження товару (з картки складу — кнопка «Надходження»): список `/warehouse/receiving` + сторінка документа `/warehouse/receiving/[id]` + створення `/warehouse/receiving/new?planned=true|false`. Дев'ятий прохід (2026-08-05) — **повний редизайн з пари документів на один**: більше нема окремих `planned`/`actual` документів — один `receiving_documents`-рядок з `is_planned` (фіксується раз при створенні) і явною машиною станів `status` (`awaiting_delivery`→`in_progress`→`completed`/`completed_with_discrepancy`), плюс лог кожного приймання по SKU (`receiving_document_item_events`, дати часткових поставок) і акт приймання-передачі (реальний PDF A4, `/api/receiving/[id]/act`). Перевірено прямими скриптами на реальній БД — обидва сценарії (просте/планове з розбіжністю) пройшли разом з guard'ами й PDF. UI зверстано, чекає звірки з людиною — див. `modules/warehouse-receiving.md`, `decisions.md`

## Беклог

- [x] Склади (settings → Склади) — таблиця `warehouses` + повний CRUD + вкладка «Адресне зберігання» переведена з bulk-генератора на ручну 3-колонкову структуру (`WarehouseBinExplorer`/`WarehouseBinColumn`): вулиця/стелаж/комірка створюються кожна окремо («+» одне значення прямо в списку, чи партія за форматом+кількістю), видалення на всіх трьох рівнях каскадно з попередженням про кількість нащадків, друк реальних PDF-етикеток (101×101мм, 5 комірок по 18мм, справжній Code128 через `bwip-js`+`pdf-lib`). «Структура адрес» злита прямо в колонки (назва рівня — інлайн перегляд+олівчик, формат — лише в блоці масового створення й запам'ятовується там, роздільник прибрано — фіксований пробіл), дубльований перемикач і нефункціональний чекбокс прибрано — усе автозберігається без картки-конфігурації. Підтверджено людиною — див. `modules/settings-warehouses.md`

- [x] `npm run db:seed`, повторно запущений на тенанті з реальними даними, повертав видалені категорії назад — виправлено (сідування категорій прибрано з `seed.ts`) — підтверджено
- [x] Категорії: успадкування від батьківської категорії (перемикачі показу, вага/розміри за замовчуванням — nullable-колонки, `null` = успадковано) + вкладка «Характеристики» на сторінці категорії — drag&drop 2 панелі (`@dnd-kit`) за макетом людини, закріплені характеристики успадковуються тим самим ланцюжком. Редагування дитини — власне значення/список назавжди. Наступний крок (окрема задача) — показ закріплених характеристик на сторінці товару динамічно за обраною категорією — підтверджено — див. `modules/settings-category-characteristics.md`

- [ ] Маски вводу (телефон `+380 XX XXX XX XX`, дробові числа через кому, дата `DD.MM.YYYY`) — нове тверде правило `conventions.md` (2026-08-03); ретрофіт усіх наявних полів: телефон постачальника (`SupplierContactsFields.tsx` → `PhoneInput`), вага (`ProductTechnicalTab.tsx`, `CategoryForm.tsx`), % складу матеріалу (`MaterialCompositionRow.tsx`), заміри (`ProductSizeChart.tsx`) → `DecimalInput`; дата вже була скрізь у потрібному форматі. Код готовий, `tsc`/`eslint` чисто, живою перевіркою в браузері не підтверджено — чекає звірки з людиною
- [ ] Авторизація (email + пароль), сесії
- [ ] Список товарів (сторінка `/products`)
- [x] Підключення PostgreSQL + Drizzle, таблиці products/product_skus (+ product_photos/product_measurements/tags), RLS — див. `db.md`
- [x] Категорії товару (settings): таблиця `categories` (ієрархія, RLS), повний CRUD (створення/редагування/видалення/зображення), зв'язок `products.category_id`, реальний лічильник товарів на категорію, хлібні крихти й вибір категорії товару за реальним деревом — див. `modules/settings.md`, `modules/products.md`
- [x] Список товарів (сторінка `/products`) — пошук/фільтри/пагінація/видалення на реальній БД, «Додати товар» створює чернетку, «Імпорт» поки заглушка; картка товару — кнопка «Створити товар»/«Редагувати» зберігає всю форму (ім'я/статус/категорія/ціни/характеристики/метадані/теги) одним запитом — підтверджено — див. `modules/products.md`
- [x] Довідник «Кольори» (settings → Довідники → Кольори): таблиця `colors` у БД (тенант-скоуповано, RLS), CRUD (квадратик-зразок = нативний color picker, синхронізований з HEX-полем, назва), 30 кольорів засіяно англ. назвами; картка товару бере кольори звідси (раніше — хардкод `COLOR_OPTIONS`) — підтверджено — див. `modules/settings.md`
- [x] Довідник «Бренди» — 100 брендів засіяно (спершу `reference_items`, згодом перенесено в `custom_characteristics` — нижче) — див. `modules/settings.md`
- [x] Довідник «Валюти» (settings → Довідники → Валюти): власна таблиця `currencies` (код/символ/позиція/знаки після коми/курс/автооновлення, лише одна базова валюта — частковий UNIQUE у БД), «Оновити курс(и)» тягне з НБУ (лише по кліку, без cron, живий виклик перевірено), UAH/USD/EUR/PLN засіяно — підтверджено — див. `modules/settings.md`
- [x] «Довідники» розбито на групи «Характеристики товару»/«Системні»; попап create+edit для власних характеристик (ввід-тегами Enter); 3 перемикачі CRM/вітрина/фільтри й кнопка-кошик (видалити можна лише користувацькі) — прямо на плитці, не в попапі; **3 перемикачі — на всіх без винятку плитках групи «Характеристики товару»** (нова генерична таблиця `reference_dictionary_flags` для «Кольорів»/«Тип тканини та матеріал»/«Інструкція по догляду»/«Розміри та заміри»); Колекції/Сезон/Бренди/Теги/Посадка перенесено з `reference_items`/`tags` у `custom_characteristics` (реальні dev-дані мігровано без втрат, «Теги» — з ризиком, підтвердженим людиною, `decisions.md`); «Кольори» перенесено з окремої сторінки в попап; курсор уніфіковано (`cursor-pointer`) на всіх клікабельних плитках — підтверджено людиною — див. `modules/settings.md`
- [ ] Довідник «Тип тканини та матеріал» (settings → Довідники): реальні таблиці `fabric_types`+`fabric_type_possible_materials`+`materials` (замість «просто назва» kind у `reference_items`), **без окремої сторінки** (пряма вказівка людини) — плитка `FabricTypesTile` у гріді «Довідники» показує наявні типи чіпами, кожен чіп/«+Додати» відкриває `FabricTypeFormDialog` (основна інформація: назва/опис/щільність/розтяжність/лицьова-тильна сторона/тактильні відчуття + «Можливі матеріали» — груповий пошуковий `Combobox` з інлайн-створенням і масовими діями «Обрати всі»/«Зняти всі», за замовчуванням усі матеріали позначені для нового типу); видалення — кнопка в тому самому попапі. Довідник «Матеріали» (CRUD за зразком «Кольорів», 55 засіяно з розбивкою по 7 категоріях) — вбудована згортана секція всередині того самого попапу, не окрема вкладка/сторінка. За прямою вказівкою людини (усвідомила помилку моделювання) **% склад, сезон, прив'язку догляду й зображення схеми до типу тканини прибрано повністю** — «тип тканини» це назва структури плетіння, а не рецепт складу, точний склад і догляд — майбутня властивість картки товару (`decisions.md`); довідник рекомендацій по догляду (`care_instructions`, назва+іконка) лишено в коді про запас, без прив'язки. «Категорії товарів» зі зразка-скріна свідомо не робили (пряма вказівка людини) — чекає звірки з людиною — див. `modules/settings.md`
- [ ] Динамічні характеристики картки товару за закріпленням категорії (Бренд/Колекція/Сезон/Посадка/Виробник/Тип тканини+Матеріал з %/Інструкція по догляду, довільні custom-характеристики) + tenant-рівневий layout панелей (drag&drop) + обов'язкова категорія для нового товару (картка заблокована без неї) — `tsc`/`eslint`/`next build`/`db:seed` чисто, **живою перевіркою в браузері не підтверджено** (пісочниця без доступу до localhost) — див. `modules/products-characteristics.md`
- [x] Довідники «Інструкція по догляду» й «Розміри та заміри» (settings → Довідники, останні 2 з 8 групи «Характеристики товару») на реальній БД. «Інструкція по догляду» — готовий бекенд (`care_instructions`) отримав плитку+попап. «Розміри та заміри» — після звірки з людиною (перша версія з єдиною таблицею-матрицею відхилена) перероблено на 2 незалежні прості довідники з тенант-керованими типами (`size_types`/`size_values`, `measurement_types`/`measurement_values`), засіяно дослідженими типовими системами розмірів/замірів; назва типу — «перегляд + олівчик» (`EditableTextRow`), нове загальне правило `ui-kit.md` — підтверджено людиною — див. `modules/settings.md`, `decisions.md`
- [x] Вкладка «Технічні дані» картки товару (Створено/Оновлено/Створив/Останній редагував/Постачальник/Артикул постачальника/Розміри (ДхШхВ)/Вага, drag&drop з tenant-рівневою персистентністю) + журнал подій товару (diff по кожному зміненому полю при автозбереженні, дата+час до секунди, актор; подія «Товар створено» при вставці) — номер поставки/замовлення свідомо не додано (модулів «Постачання»/«Замовлення» ще нема) — підтверджено людиною, живою перевіркою в браузері (Playwright) — див. `modules/products-technical-tab.md`, `decisions.md`
- [ ] Перекомпонування вкладки «Основне» картки товару за бізнес-логікою заповнення й точним макетом людини (шість проходів, 2026-08-03): розкладка 30/30/30 (Фото/Основне/Характеристики) + 62/35 (Варіанти SKU/Деталі SKU), відступи звичайні (`gap-4`); `ProductCharacteristicsPanel` (одна колонка) і `ProductPricingPanel` (окремий блок усередині `ProductInfoPanel`) з перемикачем «Для всіх розмірів однакова» (`products.same_size_pricing`); фото товару — тільки за кольором (`product_photos` видалена, міграція `0037`), без ліміту кількості (`MAX_COLOR_PHOTOS` прибрано), галерея переглядова з **основним фото** (`product_color_photos.is_main`, міграція `0039`) — велике, `object-contain` (без обрізки, у реальній пропорції), і лайтбоксом на повний розмір; таблиця варіантів більше не показує прев'ю фото (лише «Додати фото»), «+Розмір»/«+Колір» — жирніше/крупніше; **штрихкод SKU реально збережений і жорстко унікальний у межах тенанта** (`SKUDetail` → `updateSkuBarcodeAction`); **обидва ряди вирівняні по висоті** (кожен блок — `h-full`, `items-start` прибрано — виправлено й гочту `DevBlockLabel`, що розривала ланцюжок відсоткової висоти); кнопка «Редагувати розташування характеристик» переїхала в `ProductTabs` (спільний контекст `ProductCharacteristicLayoutContext`). `tsc`/`eslint`/`next build`/`db:seed` чисто, **живою перевіркою в браузері не підтверджено** — чекає звірки з людиною — див. `modules/products.md`, `modules/products-characteristics.md`, `decisions.md`
